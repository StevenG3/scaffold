# 扫描运行清单 — 门禁证据载体规则

## 归属裁定与契约缺口

本目录 `evidence/` 属于本 Change Record 的**审计元数据**——这一点由**设计方裁定**
（裁定内容：Change Record 的 `evidence/` 子目录属该记录的审计元数据），
因此其中的文件不进入 `customization-record.md` 的目标资产表。
`carrier_sweep.py` 是本记录的**证据材料**，不是可复用的 Harness 组件，
故**不在 `manifest.json` 中注册**（同一裁定）。

**这同时是一处已上报的 Skill 契约缺口**：`harness-bootstrap` Skill 的审计元数据排除清单
只列举了 `summary.md`、`spec.md`、`tasks.md`、`customization-record.md` 与 `originals/`，
**未包含 `evidence/`**。本次依设计方裁定处理，缺口与「投影文件无 bundle 相对路径」并列，
留待 B0 成熟化波次统一修订 Skill。

## 被验证的对象

`.harness/rules/project.md` §2 的门禁证据载体规则：退出码独立必记；
stdout / stderr 分流独立捕获、分别按载体规则记录；载体分支 (a) / (b) / (c)
及 (b) 内部的「无非空行」「不可呈现码点」子分支。

`carrier_sweep.py` 中的 `carrier()` 是该规则的机械实现；扫描攻击的是这个实现，
因此扫描结论的效力以「实现忠实于规则文本」为前提。该忠实性由人工逐条对照，
是本证据链中唯一不可机械化的一环，如实声明。

## 方法学声明（重要）

**抽样层能做什么、不能做什么。** 它对**每个被抽到的输入**做独立 oracle 核对——分支**与**载体值都比；
它**不能**证明的是**未被抽到的输入**。（早期版本的措辞「只能证明没有输入逃出划分、不能证明输入进入正确分支」
在加入逐样本 oracle 后已不成立，此处更正。）当年引入定向用例的理由仍然成立：
NEL / U+2028 / U+2029 这类反例即使被随机生成，也会被判为「有分支、无异常」而顺利通过——
它们的问题不是无分支可落，而是**落错了分支**。
因此定向用例（逐条携带期望分支）是必需的，随机扫描只是补充。

**只核对分支同样不够。** 载体规则的结果不止五个分支标签，还包括「记录了什么值」：
原文分支要逐字记录最后一个非空行，fallback 分支要记录正确的字节数与原始流 SHA-256，
空流要记录 `<empty>`。外部审阅用一个变异探针证明了这一缺口——把 `_digest()` 换成常量
`bytes=0 sha256=000...0` 后，47 行载体全错，而当时的扫描仍报 0 失败、digest 一字不变。

现在每个定向用例同时携带**期望载体值**，通过条件是分支与载体**双双**比中；
结果摘要也绑定了**实际载体值**。同一变异探针的当前结果：

```text
未变异：       failures=0   digest=00788ca3647b09be...（历史@a402986）
_digest 被打桩：failures=47  digest 改变
```

**但仅有定向用例的载体核对仍不足以支撑一个全称陈述。** 内部变异审阅构造了 M10：
只污染**不在当时那 69 个输入之内**的载体值（历史@50a6060；定向用例数读机器生成的 `boundary-cases.md`）。定向用例逐条仍然全绿，而摘要恰好绑定的
就是定向行，于是 digest 一字不变——「摘要能检出错误载体」这句话对定向域为真，
对定向域之外为假。

因此**随机域也获得了独立值 oracle**：`oracle_carrier()` 是同一规则的**第二次独立转写**，
它不调用 `carrier()` / `_digest()`，手工遍历字节而非使用 `split()`，
并直接由 `hashlib` 重建哈希串。`run_fuzz()` 对**每一个**随机输入同时比对分支与载体值，
两次转写只要在任意输入上分歧即计入失配并使运行见红。M10 的前后对比：

```text
整改前（无 oracle）： fuzz 不检查载体值    digest 不变      -> 绿
整改后：              fuzz_mismatches=170981  digest 改变  -> 红
                      （定向 failures 仍为 0，说明抓住 M10 的正是随机域 oracle）
```

**这一陈述随后又被外部审阅推翻，并如实收窄（历史@50a6060）。** 固定随机运行是**抽样**，
不是长度 0–6 的全域穷举——**该域规模为 `sum(256**n for n in range(7))`**，
而固定序列只产生其中很小一部分唯一输入（**当时的具体数字属 R7 轮历史记述**，绑定当轮 HEAD
历史@50a6060；实测值请运行脚本或读 `boundary-cases.md`）。审阅方污染了一个未被抽到的输入
（`b"\x00" * 6`），扫描依旧全绿。
因此「随机域上普遍成立」是假的，已撤回。

现在的陈述按层分述，每层只说它真正成立的范围：

| 层 | 覆盖 | 成立的陈述 |
| --- | --- | --- |
| 定向用例 | 人工钉住的边界输入（数量见 `boundary-cases.md`） | 每个输入的**分支与载体值**都与独立构造的期望**逐字节**相等 |
| **穷举扫描** | 长度 0–2 的**全部**输入（`sum(256**n for n in range(3))`，数量读脚本输出） | **全称成立**：该子域内每个输入的分支与载体值都与独立 oracle 一致 |
| 抽样扫描 | 长度 0–6，固定 seed 的抽取（次数由脚本常量 `ITERATIONS` 决定；抽取数与唯一输入数见脚本输出） | 每个**被抽到**的输入都经 oracle 比对；**未被抽到的输入不在证明范围内** |

全称承诺只在穷举层给出，因为那是唯一负担得起的地方；长度 3–6 不作全域承诺。

**期望载体独立构造**，不经被测代码：原文期望是手写的字面字符串；
哈希期望由**字面字节数** + 直接调用 `hashlib.sha256(raw).hexdigest()` 组成
（`hashlib` 不是被测对象，`_digest()` / `carrier()` 一次都没有参与构造期望）。
字面字节数会与真实输入长度比对，写错即在导入时报错。

**比较器本身也必须被验证。** 一个恒真的比较器会让全部用例通过、外观与正确实现无异。
生成表也带上自检结果：`--emit-markdown` 在输出头部写明该次运行的
比较器自检与 argv 自检通过数及随机域失配数——比较器一旦被污染，这些计数随之变化
（历史@bfbd546 实测：三种比较器污染各自改变表头计数），因此该产物**自带有效性证据**，
而不是仅仅声称有效。

默认模式因此内置**比较器自检**：把已知正确的期望分别变异为「错误 SHA」「错误字节数」
「错误原文末行」「错误分支」，断言四者**均被拒绝**；再加一条**对照**——未变异的期望
必须**被接受**，否则一个恒假的比较器也能通过前四项。任一自检未达预期即非零退出。

## 检查器现状（SSOT：机制描述的唯一权威）

**本节是 `check_current_numbers.py` 现状的唯一权威描述。**
记录中其他任何位置——`summary.md` 各轮叙事、`tasks.md` 条目、`protocol-runs.md` 现刻条目、
∀ 审计表、PR 正文——**一律不得复述机制现状**：要么写成指向本节的指针，
要么落在**节级历史作用域**内（该节标题已声明其轮次与 HEAD，节内现在时指当轮时点）。

理由：第十次复发证明，逐句绑定只消灭**字面串**，消灭不了**断言**——
同一条机制被散抄到多处，改一处不改其余，镜像就继续生育。
**镜像种群只能靠「单一事实源 + 作用域」整体消灭，不能靠逐句追杀。**

### 机制要点（现状）

| 项 | 现状 |
| --- | --- |
| 检查域 | Change Record 内全部 `.md`（生成表除外）全文；全部 `.py` 的**全部注释（整行与行尾行内）** 与 **真实 docstring**（module / class / function / async function 的首条语句字符串，**任意合法前缀与引号形态**——`r` / `u` / `U` / `R` 及其组合、三引号与单引号；**`b` / `f` 前缀的首条语句按 Python 定义就不是 docstring**（`ast.get_docstring` 对二者均返回 `None`），故落在域外，实现与该定义一致）。**赋值给名字的字符串、调用实参里的字符串、以及首条语句之外的字符串常量**属开放轴，不扫。语法分类由标准库 `tokenize`（COMMENT）与 `ast`（`Expr` 首条语句的 `Constant` 字符串）给出，不再手写三引号状态机；**无法 tokenize/parse 的 `.py` 一律 fail closed**，计为具名违规而非静默跳过 |
| 判定 | 同行出现「现刻标记词 + 数字」即违规；标记词表与计数词规则见脚本常量 |
| 豁免（三族，逐条列全——SSOT 不许写「等」） | **①历史绑定**：行内含 `历史@<hex>`，**整行放行**（已知宽豁免：合法历史值会连带放行同行其他数字）。**②产物不变量**：64 位十六进制指纹（脚本 SHA-256 与结果摘要共用此形）。**③结构性数字（15 条，全列）**：Change Record 目录名；`U+XXXX` 码点；`0x` 字节字面量；`Python 3.x[.y]`；`SHA-256` / `SHA256`；`第 N 步`；`第 N 轮`；表格行首编号 `\| N \|`；`§N`；轮次标签 `RN[a]`；版本标签 `vN`；`Errno N`；finding 标签（如 `I1` / `A-C1` / `B-S-I2`）；`exit N`；`退出 N`。 |
| 自检 | **两层**，缺一不可。**①语法形态矩阵** `PROSE_FORM_MATRIX`：对 `.py` 的每个语法位置逐条断言，期望值**逐行写死**、不由被测的 `prose_lines()` 生成——正向含 bare/`r`/`u`/`U`/`R` 的单行与多行 docstring、单引号形态、module/class/function/async function 四类宿主、整行/缩进/行内注释；反向含赋值字符串、调用实参、首条语句之外的字符串常量（须**不**入域）；另含一条不可解析源码须 `raises UnparsablePython` 的 fail-closed 用例。**②文件 × 记法叉积**注入：每个被扫文件 × 每个变体，全部须被捕获；变体含裸数字、逗号格式、反引号包裹、两条历史豁免逃逸样本、英文注释形态、单行 docstring 形态、**行内注释形态**。 |
| 退出码 | 覆盖域内无违规 = 0；有违规 = 1；**参数错误或 Change Record 目录缺失 = 2** |

### Group E fixture 的历史绑定（自 `carrier_sweep.py` 移入）

`carrier_sweep.py` 的 Group E 定向用例是**门禁输出快照**，取自 历史@444cc95，
**不是现刻实测**：其中的测试数与耗时会随真实运行漂移，脚本内不再重复该绑定。

原因：R16 给该注释贴历史标签时，把非 ASCII 字节引入了**被哈希存证的纯 ASCII 脚本**，
使当时全部「源码纯 ASCII」的活体声称同时为假——**历史绑定惯例与 ASCII 不变式正面冲突**。
绑定因此移到本文件（`.md` 内使用 `历史@` 无冲突），脚本注释恢复为纯 ASCII 且不含数字。

### 冻结条款（含豁免）

检测器**冻结**：不再新增检测能力（不做中文数字、不做跨行、不收紧整行放行）——
每扩张一次能力就多一片待证声称面，这是历轮复发的实测结论。

**冻结的豁免**：**已声称覆盖域内的实现缺陷，修复不受冻结限制**——
补齐实现使其符合既有声称，不是扩张声称。R17 的单行 docstring 修复即属此类：
轴表当时已写「`.py` 注释与 docstring = 覆盖」，而实现漏扫单行形态。

### 载体自由度表（轴枚举披露，R16 起为交付义务）

### 为什么改成披露，而不是再加固一轮检测器

历轮事实：**检测器每强一轮，缺陷迁一轴**——行号窗口 → 引用记法 → 文件扩展名 → 标记词汇 →
（本轮）**清单自身**。而且每轮修复**新增的声称行本身**成为下一轮的复发点：
第八次复发就发生在上一轮新写的两行核对清单里，它们在被取代后未被删除，于是继续断言
「每文件单变体轮换」与「`.md` 全域」——两句都已被同一提交证伪。

边际效用递减是结构性的：**每扩张一次检测能力，就多出一片需要被证明为真的声称面**。
因此本轮起**冻结检测器**（只修一处输出措辞），改为**把载体的每一个自由度逐条列出并标注状态**。
被标为「开放」的轴不再假装被机器覆盖；它们由**修复内容寻址纪律**与**外审的相邻缝隙探测**缓解。

### 轴表

| 轴 | 覆盖状态 | 说明与残余 |
| --- | --- | --- |
| **文件类型** | `.md` 全文、`.py` 的全部注释（含行内）与真实 docstring（任意合法前缀/引号形态）：**覆盖** | `.py` **代码行内的字符串字面量**：**开放**。代码里的数字是代码的本分，机器无法区分「常量」与「被写进字符串的现刻断言」；检查器自身的输出串就曾属于这一类（本轮以措辞修复处理，未加检测）。 |
| **语法位置** | 单行内「标记词 + 数字」共现：**覆盖** | **跨行**（标记词与数字分处两行）与**表格跨单元格拆分**：**开放**。为此建复杂检测会把检测器变成解析器，声称面进一步扩大。 |
| **数字形态** | 阿拉伯数字（含逗号分组、反引号包裹）：**覆盖** | **中文数字**（三十七）、**英文数词**（seventy-five）、拼写变体：**开放**。 |
| **时态标记** | 词表内的中英标记词：**覆盖**（词表在脚本内逐条列出，可增补） | **无标记裸数字**（句中不含任何现刻词的计数）：**开放**——这是最大的一片开放面，实弹例子即上一轮遗留的 `52 个定向用例`。 |
| **豁免条件** | 显式 `历史@<hex>` 记号：**覆盖**（刻意写下才生效） | **整行放行**：**已知宽豁免**。一个合法的历史值会连带洗白**同一行内的其他数字**。未收紧，因为按 token 定位豁免范围会再次把检测器推向解析器形态。 |
| **产物本身** | `boundary-cases.md` 由脚本生成、字节可再生比对：**覆盖** | 生成器与记录之间的语义一致性仍靠人工——`carrier_sweep.py` 的 fixture 注释就曾自称「real gate outputs」而实为快照（本轮已标注为 `历史@` 快照）。 |

### 覆盖域内的声称（唯一形态）

> 在本表标记为**覆盖**的域内，记录中不存在现刻数字断言违规——
> **构造保证**，由 `check_current_numbers.py` 可复跑验证（exit 0），
> 其失败能力由 `--self-test` 的两层证明：文件 × 记法叉积，**以及独立的语法形态矩阵**（`PROSE_FORM_MATRIX`，期望值逐行写死、不由被测函数生成，含正向的前缀/引号/位置枚举与反向的「代码字符串不得入域」用例，另含 fail-closed 用例）。
> **开放轴见上表，属已限定披露，不在该保证范围内。**

任何关于本检查的全称句一律采用上述形态；不再出现「某类缺陷在结构上不再可能」这类无界表述。

## 交付前 ∀ 句审计（常设义务）

### 本审计自身的范围（先声明，避免又一个未经检验的全称句）

上一版说「把改动产物中的**每一句**全称陈述逐条列出」——外部审阅证明**这句话本身就是假的**：
它至少漏了两条仍为假的全称陈述（digest 检测范围、跨版本确定性）。

因此本审计的完成度**限定为**：以下述**机械候选收集器的命中集**为枚举基础，**逐条人工分类**。
收集器是候选收集，不是证明；命中集之外的句子不在本表覆盖范围内。这是一个**已限定**的声明，
不是「每一句都已审计」。

**收集词表必须随声明词汇一起增长**：本轮新增的能力声明使用了「不可能」这一新词，
它不在原词表中，因此审计一度看不见它。规则：**每当交付引入新的声明词汇，
必须把该词加入收集器词表并重新收敛**——否则「收集器命中集」这一枚举基自身就会漂移。
本轮已加入 `不可能` / `must not` / `never`。

收集命令（在审阅方给出的基础上，按上述规则扩充词表）：

```sh
# 只扫**被跟踪的文本文件**；__pycache__ 等生成物会以「binary file matches」
# 混进计数，让分布表出现不是记录成员的行（R19 收敛时实际发生过一次）。
grep -rnE --exclude-dir=__pycache__ \
  'EVERY|every|ANY|any|always|never|all|全部|所有|任何|每一|每个|都会|必然|不可能|no longer possible|不再可能|must not|确定性|可复现|双自检|两项|both' \
  .harness/changes/2026-07-27-bootstrap-adoption-1
```

本表**基于最终提交态的收集器命中集 + 人工分类**：收集器在本次提交的最终内容上重跑，
命中 **468 行**，分布见下表。（上一版记录的 175 行是**跨状态混合值**——
自验会话 B 证明：同一命令在该 HEAD 上得 191、在其前一状态得 157，记录的分布两者都不匹配。
根因是收集器在编辑中途运行、随后文本又被改动。现改为**提交前最后一步**重跑并记录。）

| 文件 | 命中行数 |
| --- | ---: |
| `customization-record.md` | 5 |
| `evidence/boundary-cases.md` | 8 |
| `evidence/carrier_sweep.py` | 151 |
| `evidence/check_current_numbers.py` | 18 |
| `evidence/protocol-runs.md` | 44 |
| `evidence/run-manifest.md` | 70 |
| `spec.md` | 6 |
| `summary.md` | 121 |
| `tasks.md` | 45 |

命中集中**每一条承载能力声明的句子**都在下表拥有自己的行；本节不再使用
「其余命中不构成全称能力声明」这类概括性兜底句——那本身又是一个未经逐条检验的全称句。
未成行的命中均为标识符、代码关键字、历史整改记录的叙述文字或英文虚词，
其判定由人工逐条完成，属下文「未覆盖与已知不足」所述的不可机械消除环节。

### 分类口径（采用审阅方的五分法）

1. **构造保证**；2. **穷举证明**；3. **仅对已执行定向用例成立**；
4. **仅对已抽样输入成立**；5. **没有证据或已被反例推翻**（须撤回或收窄）。

### 审计表

> **作用域声明（一次性）**：本表**每一行都是当轮陈述**，行首编号对应其提出轮次；
> 行内文字描述的是**该轮时点**的机制，不是现状。机制现状见上方「检查器现状（SSOT）」节。
> 此后不再为表内单行追加历史标签。


| # | 全称陈述 | 分类 | 依据 / 限定 |
| --- | --- | --- | --- |
| 1 | 「任何字节串都恰好落入一个分支，且该分支要求记录的对象一定存在」 | **构造保证** | 沿「空/非空 × 可否严格解码」切分，(b) 内再二分；穷举性来自划分方式本身。 |
| 2 | 「集合外的码点均可原文记录」 | **已撤回**（类 5） | R5b 撤回：集合是本项目固定约定，非对补集的断言；集合外不可见码点会被原文记录，属已知残余风险。 |
| 3 | 「每条门禁记录 exit + stdout 载体 + stderr 载体」 | **构造保证** | 载体规则对任一确定字节串是全函数，两个流各套用一次。 |
| 4 | 「期望值独立于被测代码」 | **已限定** | 仅在「可信计算基披露」列出的共享项之外成立。 |
| 5 | 「两份不可呈现码点集一致」 | **构造保证** | 导入时逐元素交叉断言；单侧改动 `AssertionError`。双侧同改不覆盖，已披露。 |
| 6 | 「五个分支标签互异且两侧一致」 | **构造保证** | 唯一性检查 + 成对交叉断言；S1/S2 实测 exit 1。 |
| 7 | 「参数域已闭合」 | **已限定** | 仅认证**已交付的 argv 空间**；新增 flag 必须同步扩充自检（上一轮的 help 入口即为反例）。 |
| 8 | 「长度 0–2 的每个输入，分支与载体值都与独立 oracle 一致」 | **穷举证明** | 长度 0–2 的全部输入实跑（数量与失配数见脚本输出）。本交付**唯一**的全域承诺。 |
| 9 | 「扫描会在载体值出错时见红，随机域上普遍成立」 | **已撤回**（类 5） | R7 撤回；抽样非穷举。 |
| 10 | 「比较器会拒绝错误期望」 | **已限定** | 限定为自检**枚举的变异类**（错误 SHA / 字节数 / 原文末行 / 分支 / 空载体 / 单侧集合或标签编辑）；枚举之外不作承诺。 |
| 11 | 「生成表自带有效性证据」 | **已限定** | 表头计数只证明**该次运行**的自检与各层状态。 |
| 12 | 「本脚本仅依赖标准库」 | **构造保证** | 导入项 `hashlib`/`os`/`random`/`re`/`shutil`/`sys`/`tempfile`，可机械核对。 |
| 13 | 「源码为纯 ASCII」 | **构造保证** | 可机械核对。 |
| 14 | 「让实现记录错误载体，摘要都会变化」 | **已收窄**（原属第五类） | 摘要绑定**定向行的实际载体** + **各层汇总计数**；**未被执行输入上的错误不在检测范围内**。审阅方 `b"\x00"*6` 探针实测 `digest_changed=False`（历史@50a6060），与新措辞一致。 |
| 15 | 「任何 Python 3.9+ 重跑结果一致」 | **已收窄**（原属第五类） | 限定为**本环境的实测解释器版本**（值见下方参数表） + 固定 seed 下逐位可重现；**不对跨 Python 版本作承诺**（CPython 文档：多数 random 算法可能随版本变化，跨版本保证不覆盖 `randint`/`getrandbits` 组合）。 |
| 16 | 「A corrupted comparator changes these counts」 | **已限定** | 限定为自检枚举的比较器变异类，不再使用「任意污染」语气。 |
| 17 | 「抽样层只能证明没有输入逃出划分」 | **已更正**（原措辞与实现不符） | 抽样层对**每个命中输入**做独立 oracle 的分支 + 载体核对；它**不能证明的是未抽中的输入**。 |
| 18 | 「本审计枚举了每一句全称陈述」 | **已限定** | 限定为「收集器命中集 + 人工分类」；命中集之外不在覆盖范围内。 |
| 19 | 「认证失败的输出永远不会落到正式路径，也永远不会 exit 0」 | **构造保证** | 所有出口由唯一认证状态派生；三类层失败 + 两条会话反例实测拒绝写入、正式文件未变（历史@b9a12a0）。 |
| 20 | 「任何前缀不在注册表中的失败标签都会使认证 fail closed」 | **构造保证** | 兜底层「未注册自检失败」+ 层集合与注册表一致性断言；注入未注册前缀实测 exit 1。 |
| 21 | 「两个流永不拼接」 | **构造保证** | 规则中不存在合并输出这一对象；stdout/stderr 分别独立捕获与记录。 |
| 22 | 「原子写入使旧表在完整新表就绪前不受影响」 | **已限定** | 限定为 `os.replace()` 在 POSIX/Windows 上的原子性；不覆盖磁盘故障、文件系统异常与并发写入者。 |
| 23 | 「项目层内容永不回灌分发包」 | **构造保证（机械可验）** | `tests/test_template_contract.py` 的 token 扫描 + 每轮 `git diff main -- template/` 为空。 |
| 24 | 「`-h`/`--help` 必须是唯一参数，其余组合一律拒绝」 | **构造保证** | 完整 argv 先校验后选模式；argv 自检逐条覆盖（项数见脚本输出），外部进程实测多类组合均 exit 2。 |
| 25 | 「默认模式是唯一以退出码承载判定的模式」 | **已撤回** | 自 R8 起 `--emit-markdown` 同样承载判定；模式矩阵为唯一权威，相关句子已全域删除。 |
| 26 | 「新增一类检查而忘记注册也会 fail closed」 | **已收窄并补强** | 原句为假（未注册的产出者根本不执行）。现声明收窄为四条实际保证，其中「未注册产出者 → 导入期断言失败」为本轮新增能力，实测 exit 1。 |
| 27 | 「CERTIFIED 表头不可能压在 FAIL 行之上」 | **构造保证 + 独立守卫（两出口一致）** | 裁决由渲染出的行 `all(ok)` 现场派生（无可漂移布尔值）；守卫在 emit 与默认两个出口都运行，且只读哨兵界定的认证视图（载荷透明）。**残余（两类，均已验证）**：其一，连印出的数字一并改掉的整体伪造；其二，**注册类零执行**的渲染——证言计数相等、无非零数字、无 FAIL 单元，**守卫看不出矛盾**，它由 `certification_state()` 的兜底层（执行数为 0 即 fail closed）拦下。这两道防线**分工不同**：守卫抓「渲染自相矛盾」，认证状态抓「事实本身不成立」；不要把任一方当作另一方的替代。 |
| 28 | 「自检不读取任何共享环境状态」 | **已限定** | **已闭合的共享状态（枚举）**：`$TMPDIR` 下的共享名字、cwd、硬编码绝对路径——三种敌对环境实测均不影响认证。**未闭合但已承认**：进程 `umask` 决定 `mkdtemp()`/`mkdir()` 能否建出可用目录。该情形**不再是崩溃**：构造失败被捕获为 argv 层内一条**具名失败检查**，认证块照常渲染、退出 1、原因可读（受限 umask 下实测无 traceback、NOT CERTIFIED 且点名 argv 层；常规 umask 全绿）。 |
| 29 | 「定制记录覆盖每一个目标资产」（`customization-record.md`） | **已限定** | 覆盖范围限定为 Skill 定义的目标资产类别（`rules/` / `wiki/` / manifest 组件注册 / 随包发行文件的改动）；审计元数据按契约排除，`evidence/` 的归属由设计方裁定另行记录。 |
| 30 | 「输入与载体完整呈现、从不截断」 | **构造保证** | 生成器对输入与载体一律使用完整 `repr`，无长度截断分支；可由生成表逐行核对。 |
| 31 | 「载荷透明：定向用例的输入即使字面是 `**FAIL**` / 缺陷计数串 / 认证标记 / `NOT CERTIFIED` / `does NOT certify` 散文 / **起始哨兵字面量**，也不影响认证」 | **构造保证** | 该保证由**两处**共同成立，缺一不可：守卫只解析哨兵界定的认证视图，**且** `selftest_certification()` 的每处文本断言同样只对该视图取值（R10b 之前它读整份文本，正是载荷翻转认证的入口）。**六种**载荷已固化为定向用例（含起始哨兵字面量），实测 PASS、两出口 exit 0、生成表可写且逐字节稳定。 |
| 32 | 「载荷行不可能等于哨兵行」 | **构造保证** | 载荷行要么是以 `\| ` 开头的表格行、要么是散文行，均不可能与整行哨兵字面量相等（起始哨兵字面量已作为定向用例固化，实测 PASS）；守卫另要求两个哨兵各出现恰好一次，**并校验视图的结构完整性**——marker 唯一、layer 行与 `CERT_LAYERS` 完整同序、attestation 与 `CHECK_CLASSES` 完整同序，因此以下**七种**形状均被拒（实测，逐一具名）：premature end、late begin（起始哨兵后移）、removed begin（起始哨兵删除）、空视图、仅 marker、缺 layer、缺 attestation。**残余**：该保证依赖渲染器不新增「整行输出任意字节」的分支；若将来新增，须回本行复核。 |
| 33 | 「本次运行的自检计数写入表头，因此产物自带有效性证据」 | **已限定** | 只证明**该次运行**的自检与各层状态；不对未来运行、也不对枚举之外的变异类作承诺。 |
| 34 | 「陈旧数值这一类缺陷在结构上不再可能」 | **已缩域** | 原句由「本节之后」的**有界**前提推出**无界**结论，被会话 C 在 `run-manifest.md:49` 的残值当场证伪（第五次复发）。现改为句式禁令 + 全文域：除机器生成产物外，记录任何 `.md` 中「现刻标记词 + 数字」同行出现即违规，由 `check_current_numbers.py` 全文机械核查；该命令**已做逐文件、逐记法的变异验证，能失败**。能声称的只是「白名单之外的数字会被这条命令抓住」，不是「不可能出现」。 |
| 35 | 「白名单 + 当前态小节窗口足以拦住陈旧数值」 | **已撤回** | 该方案有两个被击穿点，均由会话 C 实证：其一，**窗口**只覆盖少数行号区间，`summary.md` 整个不在域内、`run-manifest.md` 大部分不在域内；其二，**反引号置盲**使任何数字加一对反引号即绕过检测，而反引号恰是本记录的主流数值写法。现改为**句式禁令 + 全文域**：除机器生成产物外，任何 `.md` 中「现刻标记词 + 数字」同行出现即违规；检查域为整目录全文，豁免仅两类且机器可判（历史绑定行 = 含「历史」且含七位以上十六进制 HEAD；两条产物不变量）。 |
| 36 | 「句式禁令 + 全文域已闭合陈旧数值」（R14 方案） | **已限定 → 再收敛** | R14 只沿**被击穿的轴**闭合（行号窗口、引用记法），留下两条**开轴**：**文件扩展名**（只扫 `.md`，于是第七次复发藏在被 SHA 认证的 `.py` 注释里）与**标记词汇**（词表漏掉语料主导的现在/现行/实测/本轮/最新/共）。本轮闭合方式：域扩至 `.md` 全文 + `.py` 的注释与 docstring 行；词表并入中英文并对计数词要求量词共现；历史豁免由「同行共现」改为**显式绑定记号** `历史@<hex>`（旧式可被引用摘要走私，两条逃逸样本已固化为自检）。**教训：域闭合必须枚举全部载体轴，而不是仅闭合被击穿的那一条。** |
| 37 | 「检查器已闭合陈旧数值这一缺陷类」（R15 及以前的各版形态） | **已缩面 + 轴枚举披露** | 历轮证据表明检测器扩张边际递减，且每轮新增的声称行自身成为下一轮复发点（第八次复发即发生在核对清单的两行废止行里）。R16 起**冻结检测器**，声称统一缩为「在轴表标记为覆盖的域内无违规（构造保证，exit 0 可复跑）」，开放轴（`.py` 代码字符串、跨行/跨单元格、中文数字与英文数词、无标记裸数字、`历史@` 整行放行）逐条披露于**载体自由度表**，由内容寻址修复纪律与外审相邻缝隙探测缓解。 |
| 38 | 「每个修复必须对照其触碰文件的**全部**既有不变式」（R19 新增纪律） | **规范句，非能力声称** | 这是对交付行为的**义务**，不是对系统状态的断言，因此不可能被「实测为真」；它的执行力来自落地清单里那条**能变红**的 ASCII 不变式行，而不是这句话本身。R19 的复发正说明：写成散文的义务不产生约束力。 |
| 39 | 「扫描词表必须与记录实际使用的语言同域」（R19 新增纪律） | **规范句 + 已限定** | 同上为义务句。可核查的部分仅限「本轮扫描记录中确有英文关键词行」；**不**声称词表已覆盖记录中的全部语言或全部措辞——那正是被反复证伪的无界形式。 |
| 40 | 「`.py` 的注释与 docstring 已在检查域内」（R14–R27 各版形态） | **已重述并使实现等于声明** | 外审第 12 轮证明手写三引号状态机与该声明双向不符：`u`/`U`/`R` 前缀的真实单行 docstring 漏报、行内注释漏报，而**赋值给名字的多行字符串反被扫入**——与轴表「代码字符串=开放」正相反。属冻结豁免允许的「已声称覆盖域内的实现缺陷」。现改用 `tokenize` + `ast`，并以写死期望的语法形态矩阵双向验证；不可解析的 `.py` fail closed。 |
| 41 | 「语法形态矩阵覆盖全部 Python 语法位置」 | **已限定** | 矩阵是**枚举**，不是证明：它覆盖已列出的前缀、引号形态与四类 docstring 宿主，以及四条反向用例；未列出的语法位置不在其覆盖内。但域的判定已交给标准库，故实现不再依赖该枚举的完整性——枚举只用于**证明实现符合声明**。 |

### 未覆盖与已知不足（诚实声明）

- 收集器是**正则关键词匹配**，不含语义分析：一个不含上述任何关键词的全称句会被漏掉；
- 「人工分类」是人的判断，与本证据链中「规则文本 → 两次转写」同属不可机械消除的一环；
- 因此本审计**降低**了遗漏全称陈述的概率，**并未消除**它。

## 认证层的单一来源（注册表驱动）

自验会话 A 与 B 各自独立复现了同一个 Critical：`certification_state()` 用**三个硬编码前缀**
分桶自检失败，而 `"certification:"` 不匹配其中任何一个，**被静默丢弃**——
认证自检失败时仍得到 `certified: YES`、exit 0，并覆盖正式表为 CERTIFIED。
同时 `CERT_LAYERS` 是一段**死代码**，从未被消费，因此「层集合」与「分桶集合」可以各自漂移。

整改：`CHECK_CLASSES` 是**唯一来源**，`CERT_LAYERS` 由它派生并被 `certification_state()`
实际消费（不一致时抛 `AssertionError`）；新增**兜底层**「未注册自检失败」——
任何前缀不在注册表中的失败标签一律使认证失败。**本机制实际保证的四条**（不多也不少）：

1. 已进入 `selftest_failures`、但前缀不在注册表的失败标签 → fail closed；
2. 已注册、但产出者缺失或执行数为 0 的类 → fail closed；
3. 写了符合命名约定（`selftest_*` / `_selftest_*`）却**未注册**的产出者 → **导入期断言失败**；
4. 渲染文本自相矛盾（FAIL 行 + CERTIFIED 标记等）→ 产物级守卫拒绝写入并 exit 1。

上表第三条是当轮新增（历史@c008666）：此前一个忘记注册的产出者根本不会被执行，也就没有失败标签可供兜底层捕获；
现在导入期扫描模块命名空间，凡符合产出者命名约定的可调用对象都必须出现在注册表中。
实测：在副本中定义未注册的 `selftest_forgotten` → 导入即 `AssertionError`，exit 1，不写任何文件。

实测两条反例（会话 A/B 的原始构造）：

```text
认证自检中有一项失败      -> default exit 1, --emit-markdown exit 1, PATH 被拒绝, 正式文件未变, 出现「does NOT certify」
注入未注册前缀的失败标签  -> default exit 1, --emit-markdown exit 1, PATH 被拒绝, 正式文件未变, 出现「does NOT certify」
```

## 认证裁决的结构（无共享布尔值）

`certification_state()` **只返回 layer 事实**，不返回裁决布尔值。每个出口都从**它自己渲染的那些行**
用 `all(ok)` 派生裁决，因此「CERTIFIED 表头压在 FAIL 行之上」在构造上不可能出现——
表头就是那张表算出来的。

外部审阅的原始变异（包住 `certification_state`、保留 layer 表、只把布尔值强制为 True）
**已无对象可污染**：不存在这样一个布尔值。实测该形状现在得到
`rc=1`、横幅为首行、无 CERTIFIED 标记、FAIL 行仍在表内。

此外还有一道**与派生逻辑无共享**的产物级守卫 `artifact_guard_violations()`。**它在两个出口都运行**：`--emit-markdown` 写入前，以及默认模式打印认证块之后——两者渲染同一个 `render_certification_block()` 的输出，因此守卫**与出口无关**。（自验会话 A 发现的正是这一点：此前守卫只在 emit 出口运行，同一个伪造在 emit 被拒 10 项，在默认模式却 rc=0 全绿。）守卫内容：
它只读**渲染后的文本中被哨兵行界定的那一段认证视图**
（`<!--CARRIER-SWEEP-CERTIFICATION-VIEW-BEGIN-->` / `-END-->`，各须恰好出现一次），
**从不读取载荷表格**——因此定向用例的输入即使字面上就是 `**FAIL**`、`7 failures`
或认证标记本身，也不会触发守卫（这三种载荷已作为定向用例固化，实测全部 PASS、
默认与 emit 均 exit 0、生成表可写且逐字节稳定）。
在该视图内发现以下任一情形即拒绝（emit 出口拒绝写入，默认出口打印拒绝原因），两者均 exit 1：
「`**FAIL**` 行与 CERTIFIED 标记并存」「两个标记并存」「无标记」「未认证却非横幅开头」
「CERTIFIED 在场却有 `X/Y passed` 且 `X != Y`」「CERTIFIED 在场却有非零缺陷计数」「CERTIFIED 在场却仍带不认证横幅」「CERTIFIED 在场却仍写 does NOT certify」。
渲染器与守卫**共用** `FAIL_CELL` / `PASS_CELL` 常量，并有一项自检实测「未认证渲染确实含 FAIL 令牌」——
消除守卫与渲染字面量各自漂移的隐患。

**残余风险（按层精确声明）**。上一版把残余笼统写成「伪造 layer 事实后渲染内部自洽」——
内部验证者证明**这个前提是错的**：伪造 layer 行确实不留 `**FAIL**` 单元，但**证言计数**
（`X/Y passed`）来自真实的检查结果，因此一张写着 `8/23 passed` 的 CERTIFIED 表并不自洽。
守卫已据此补上**证言完整性检查**：CERTIFIED 标记在场时，任一 `X/Y passed` 若 `X != Y` 即拒绝。

按层给出**当前**的可捕获性：

| 伪造对象 | 是否被捕获（**两个出口一致**） | 依据 |
| --- | --- | --- |
| 定向层（把 directed layer 的 `ok` 改真） | **捕获** | 用例表逐行由真实比对渲染，失败行仍带 `**FAIL**` 单元 |
| 四类自检层（改 `ok` 为真） | **捕获** | 证言计数由真实检查结果生成，`X != Y` 触发完整性检查 |
| 未注册失败标签层 | **捕获** | 该层为真时残留标签会出现在层详情，且伴随自检失败计数 |
| 穷举层 / 抽样层（改 `ok` 为真） | **捕获** | 守卫扫描层详情数字：CERTIFIED 在场时任何非零 `N mismatches` / `N failures` / `N undefined` / `N unrecognised` 即拒绝 |

**本轮再次收紧**：守卫现在还扫描层详情里的数字——CERTIFIED 在场时，任何
`N mismatches` / `N failures` / `N undefined` / `N unrecognised`（`N != 0`）即拒绝。
验证者的两个外科式伪造（穷举与抽样各挂一个非零失配在 CERTIFIED 下）实测均被拒（历史@b9a12a0）。

守卫另加两项可见矛盾检查：CERTIFIED 在场却仍带「不认证」横幅、或仍写着 `does NOT certify`，
一律拒绝（会话 B 的 marker + FAIL + 数字三重伪造仍会留下这两处可见矛盾）。

另有一类**结构校验看不见、但不可达**的形态：视图中混入一条**未注册的良性行**
（既不在 `CERT_LAYERS`、也不是 attestation 行）。结构校验按注册表比对，不枚举「多余行」，
因此看不见它；但它**无法用来伪装成功**——非零数字与 `**FAIL**` 单元仍分别被数字扫描与
FAIL 扫描拦截，而把真实失败行替换成良性行会立刻造成「缺 layer」并被拒。

**当前真正剩下的残余只有一种**：
一次**把印出来的数字一并改掉**的整体伪造——即渲染文本从头到尾自洽地说谎。
那已经不是「守卫看不见矛盾」，而是**根本没有矛盾可看**，与「oracle 本身说谎」
（两次转写被同时改成同一个错误答案）属于同一类：任何只读产物的自洽性检查都无从发现，
必须靠独立复现来发现。

## 可信计算基披露（哪些东西仍被共享）

「独立」不是一个绝对属性，而是相对某个共享基线而言的。外部审阅证明了不披露的代价：
当期望与 oracle 都读取被测代码的 `CARRIER_EMPTY` 与 `UNRENDERABLE_SET` 时，
把 `CARRIER_EMPTY` 改成 `"<wrong>"`、或从集合里删掉一个未被定向用例覆盖的成员
（U+0001），被测实现已经违反规则，而全部检查仍然全绿。
因此本节精确列出**仍然共享**的东西，独立性声明只在此披露之外成立。

### 仍然共享（共因风险真实存在）

| 共享项 | 说明与残余风险 |
| --- | --- |
| Python 运行时与解释器 | 被测实现与两个检查侧跑在同一进程、同一 CPython 上。解释器层面的缺陷会同时影响双方。 |
| `hashlib.sha256` | 期望值与被测实现都用它。若它本身错误，双方同错且不可见。这是刻意接受的：它不是被测对象。 |
| `bytes.decode("utf-8")` 的严格性 | 分支 (c) 的判定双方都依赖同一个解码器。 |
| **两份不可呈现码点集的交叉断言** | `UNRENDERABLE_SET`（被测侧）与 `ORACLE_UNRENDERABLE_SET`（检查侧）是**两次独立转写**，导入时交叉断言逐元素相同：改动**任一侧**立即报错。但**同时改动两侧**是真正的共因，本方案不覆盖——这是已知且被接受的残余风险。 |
| 分支标签的语义 | 两份标签字面量内容相同（必须相同，否则比较失效）。若**同时**修改两侧且保持一致，分类语义可以整体改变而不被察觉——与码点集同属「双侧同改」类残余风险。 |
| 规则文本 → 实现的人工转写 | `carrier()` 与 `oracle_carrier()` 都是人按规则文本写出来的。两者写法刻意不同（手工遍历字节 vs `split()`、两份集合字面量），但**同一个人的同一处误解**可能同时进入两份转写。这是本证据链中最根本、也无法机械消除的一环。 |

### 已经不再共享（拆分记录）

| 项 | 拆分方式 |
| --- | --- |
| 空流载体常量 | 检查侧使用独立字面量 `EXPECTED_EMPTY_LITERAL`，**绝不**读取被测的 `CARRIER_EMPTY`；把后者改成 `"<wrong>"` 实测使默认模式 **exit 1**。 |
| 不可呈现码点集 | oracle 使用自己的第二份字面列表；单侧删除 U+0001 实测在导入时即 `AssertionError`，**exit 1**。 |
| 哈希载体串与原文期望 | 定向期望由字面字节数 + 直接 `hashlib` 调用构造，从不经 `_digest()` / `carrier()`。 |
| 分支标签常量 | 检查侧拥有自己的 `ORACLE_BRANCH_*` 字面量，定向期望也用它们；导入时**成对交叉断言**且**各自内部唯一性检查**。两项缺一不可：内部审阅的 S2 变异把两个标签改成同一个字符串，五分划分**静默塌缩成四分**而没有任何一对不相等——只做成对比较抓不到它。实测：S1（单侧改名）与 S2（标签碰撞）现在均在导入时 `AssertionError`，**exit 1**。 |

## 定向用例的来源

| 组 | 来源 |
| --- | --- |
| A | `docs/process/invariant-closure-design.md` §2.3 声明的对抗字节域，**逐项**对应 |
| B | 规则不可呈现码点集的**每个族**各取代表，**外加集合外的可见邻居**（见下） |
| C | 「行」的字节级定义 |
| D | UTF-8 可解码性边界 |
| E | 本 Change Record 实际记录的七条门禁输出回放 |

**关于 B 组的可见邻居**：邻居用例的作用是证明规则「不误伤」。因此邻居必须是**确实可见**
（或可见地占位）的**已分配**码点：本次采用 U+00A0、U+2010、U+2027、U+202F、U+3000。
**不使用未分配码点作邻居**——规则对补集不作任何断言，拿未分配码点当「应可原文记录」的
期望值等于替规则声称它并未声称的东西。

## 算法与参数

| 项 | 值 |
| --- | --- |
| 定向用例 | 逐条携带**期望分支与期望载体值**；两者都比中才算通过（数量见 `boundary-cases.md`） |
| 随机域 | 全部 256 个字节值，长度 0–6（均匀） |
| 固定 seed | `SEED = 20260727`（写死在脚本常量中） |
| 迭代次数 | 由脚本常量 `ITERATIONS` 决定（值见源码，勿抄录） |
| 依赖 | 仅标准库（`hashlib`、`os`、`random`、`re`、`shutil`、`sys`、`tempfile`） |
| 确定性（**已限定**） | 无时钟输入，不读取任何**共享**环境状态。argv 路径自检确实做真实文件系统操作，但只在它自己用 `mkdtemp()` 创建、随后删除的**私有目录**内进行，因此环境中的既有文件无法影响任何结果（内部验证者此前用 `mkdir "$TMPDIR/out.md"` 翻转过认证——那正是因为旧检查读了共享名字）。**在下方记录的解释器版本上**，固定 seed 下逐位可重现（样本集与结果摘要均一致）。**不对跨 Python 版本作任何承诺**：样本由 `random.randint()` 与 `getrandbits()` 组合生成，CPython 文档明确大多数 random 算法可能随版本变化，其跨版本保证只覆盖兼容 seeder 下的 `random()`，不覆盖本脚本使用的调用组合。见 <https://docs.python.org/3/library/random.html#notes-on-reproducibility> |
| 解释器版本（本次测量环境） | `Python 3.14.5`（`python3 --version`） |
| 源码编码 | 纯 ASCII：不可呈现码点一律以 `\uXXXX` 转义出现，diff 可读 |
| 脚本 SHA-256 | 见本文件「本节保留的唯一两个不变量」小节——**该值在全库只允许存在一处**，此处不复制 |

## 复现与再生成命令

```text
# 扫描（定向 + 随机），打印结果摘要
python3 .harness/changes/2026-07-27-bootstrap-adoption-1/evidence/carrier_sweep.py

# 再生成 boundary-cases.md（推荐：原子写入，脚本自己落盘）
python3 .harness/changes/2026-07-27-bootstrap-adoption-1/evidence/carrier_sweep.py \
    --emit-markdown .harness/changes/2026-07-27-bootstrap-adoption-1/evidence/boundary-cases.md

# 仅查看/diff（流向 stdout，不落盘）
python3 .harness/changes/2026-07-27-bootstrap-adoption-1/evidence/carrier_sweep.py --emit-markdown

# 用法
python3 .harness/changes/2026-07-27-bootstrap-adoption-1/evidence/carrier_sweep.py --help

# 红基线：以整改前的判据跑同一批定向用例，预期见红
python3 .harness/changes/2026-07-27-bootstrap-adoption-1/evidence/carrier_sweep.py --baseline
```

### 退出码语义（按模式，逐模式明确）

| 模式 | 退出码含义 |
| --- | --- |
| 默认（无参数） | **判定语义**：0 当且仅当全部 layer 事实为 PASS。layer 集合 `CERT_LAYERS` **由检查类注册表 `CHECK_CLASSES` 派生**（非手工维护）：定向层、四类自检（比较器 / 共因 / argv / 认证）、长度 0–2 穷举层、抽样层（含 undefined），外加**「未注册自检失败」兜底层**。任一层 FAIL 即 exit 1。 |
| `--baseline` | **报告模式**：退出码**不承载判定语义**，只表示运行本身完成（正常为 0）。该模式**预期见红**，若以失败数决定退出码，读者会把「基线如期见红」误读成「扫描失败」。 |
| `--emit-markdown`（认证通过时） | 输出正式表，退出 0。**该模式承载判定**（不是报告模式）。 |
| `--emit-markdown`（**认证失败时**） | **拒绝写入正式文件**并 exit 1（给出失败层清单）；无 PATH 时只向 stdout 打印带「不认证」横幅的诊断表，同样 exit 1。**报告能力与成功语义彻底分离**：认证失败的输出永远不会落到正式路径，也永远不会 exit 0。 |
| 未知参数 | 拒绝执行，stderr 打印错误与用法行，退出 **2**。 |
| **模式组合** | `--baseline` 与 `--emit-markdown` **互斥**，同时给出即拒绝执行、退出 **2**（两种顺序均拒绝）。此前二者可同时启用，会把 baseline 的失败与在用判据的 fuzz 计数混进同一个 digest，并按文档给出的重定向方式静默覆盖正式 `boundary-cases.md`。 |
| 重复同一 flag | 视为同一模式，接受。 |
| `--emit-markdown PATH`（认证通过时） | 接受；表由脚本**原子写入**（同目录临时文件 + `os.replace()`）。此前文档给出的是 shell 重定向，shell 一打开文件就截断，运行中途失败会在 Change Record 里留下零长度或半截的正式表。 |
| 无 `--emit-markdown` 的路径参数 / 多个路径 | 拒绝，退出 2。 |
| `-h` / `--help`（**单独出现**） | 用法打印到 **stdout**，退出 **0**。 |
| help × 任何其他 token | **拒绝，退出 2**。此前 `parse_args()` 见到 help 即提前返回，早于未知参数、互斥模式与路径校验，于是 `--help --bogus`、`--help --baseline --emit-markdown`、`-h stray.md` 全部 exit 0——新增的 help 入口把已声称闭合的参数域重新打开了。现在**先校验完整 argv，再选择模式**，且 help 必须是 argv 中的唯一 token（`-h --help` 也拒绝）。 |

参数域闭合：任一 argv 要么落入四个模式之一（default / baseline / emit / help），
要么被拒绝并退出 2。**该闭合只认证已交付的 argv 空间：新增任何 flag 都必须同时扩充 argv 自检，
否则旧的闭合结论对新空间不再成立**（本轮的 help 入口正是这样重新打开了上一轮宣布闭合的参数域）。默认模式内置的 **argv 自检**逐条断言上述行为（项数读脚本输出），
包括两种模式组合顺序、路径校验，以及 help × 未知参数 / × 各模式 / × 路径 / × 三元组合。

**模式矩阵（唯一权威）**：

| 模式 | 是否承载认证判定 | 退出码含义 |
| --- | --- | --- |
| 默认（无参数） | **是** | 0 = 全部 layer PASS；1 = 任一 layer FAIL |
| `--emit-markdown [PATH]` | **是**（自 R8 起） | 0 = 认证通过（有 PATH 时写入正式表）；1 = 认证失败（**拒绝**写 PATH；无 PATH 时输出带横幅的诊断表） |
| `--baseline` | 否，报告模式 | 恒 0（除非运行本身出错）；**预期见红**，永不写文件 |
| `-h` / `--help`、参数错误 | 否，协议状态 | help = 0；未知参数 / 互斥组合 / 非法路径 = 2 |

**每一格均已实测**（非法路径此前为假：不存在的目录与目录型目标会抛 `OSError` 栈回溯并 exit 1，
而认证正在 PASS——既推翻了矩阵，又把留给「判定失败」的 exit 1 花在了 IO 错误上）：

```text
default                                   exit 0
--baseline                                exit 0
--help                                    exit 0
--emit-markdown（无 PATH，认证通过）      exit 0
--emit-markdown PATH（认证通过）          exit 0，写入
--emit-markdown（认证失败）               exit 1，无 PATH 时输出横幅诊断表
--emit-markdown PATH（认证失败）          exit 1，拒绝写入
--help --bogus / -h stray.md              exit 2
--emit-markdown ""  /  "   "              exit 2
--emit-markdown /no-such-dir-4f2a/out.md  exit 2（父目录不存在）
--emit-markdown /tmp                      exit 2（目标是目录）
```

默认模式与 `--emit-markdown` 承载**同一个**认证判定；不存在「只有默认模式才算数」的说法。

## 结果（**去镜像**：当前态数字不再手抄）

第 11 轮把 Git 可导出的统计量从记录中删除；本轮把同一规则推到**脚本可导出**的一类。
定向用例数与失败数、四类自检各自的项数、穷举总数与失配数、抽样次数 / 唯一输入数 /
失配数、五个分支的命中直方图、各探针的 digest——**全部由脚本在运行时产出**。
手抄一份进记录，就必然出现「脚本改了、抄本没改」的漂移；这已是同一缺陷的第四次复发。
因此**它们不再出现在本节**。

**要看当前值，请运行命令，或读机器生成的产物**：

```sh
# 全部当前态数字（层状态、四类自检计数、穷举与抽样、分支直方图、结果摘要）
python3 .harness/changes/2026-07-27-bootstrap-adoption-1/evidence/carrier_sweep.py

# 红基线（整改前判据下的定向结果）
python3 .harness/changes/2026-07-27-bootstrap-adoption-1/evidence/carrier_sweep.py --baseline

# 逐条用例与层状态表（机器生成，随交付入库）
sed -n '1,40p' .harness/changes/2026-07-27-bootstrap-adoption-1/evidence/boundary-cases.md
```

**结果读机器生成的 `boundary-cases.md`**：它由 `--emit-markdown` 生成、随交付入库、
并被守卫校验；它才是当前态数字的权威载体。

### 本节保留的唯一两个不变量（**全库唯一抄本**）

只保留**描述产物内容、而非描述「哪个提交 / 哪次运行包含它」**的两个值——
它们不参与任何自指追逐：

| 项 | 值 |
| --- | --- |
| 脚本 SHA-256 | `067d1c7bb7af8594adf83ff20599b221f3358571572f98d3de4e555a925a4338` |
| 结果摘要（result digest） | `20ad0f5005822570f955e0542e7446b21f962b187c630b08d75d5f1d7f4a6b6d` |

> **历史小节保留其带时点标注的数字**（那些是当轮事实，不是当前态断言）。

**单点化（R20 起）**：以上两个值是**手抄**的——脚本一改，抄本就旧，而检查器的
豁免②「整行放行任意 64-hex」使它们**结构性无守护**。R19 正是死在这里：
脚本改了，三处抄本只同步了一处，其中一处失同步的抄本就在这段警告「脚本改了、
抄本没改」的文字**正下方**。因此规定：

1. 这两个值在整个 Change Record 里**只允许存在于本小节**；其余位置一律写指针。
2. 唯一例外是**机器生成**的 `boundary-cases.md`（结果摘要由 `--emit-markdown`
   写入）与 `summary.md` 里**逐字粘贴的运行输出**——它们不是手抄，是产物。
3. 落地核对清单里配**会变红的守护行**（行数与内容以清单为准，此处不复述）：
   比对不变量节的值与当场 `shasum`、数全库出现次数、并对结果摘要的出现文件逐一具名。
   **手抄值的唯一可行守护，是让「有几处抄本」本身成为一个可执行的期望值。**

### 数字豁免的权威位置（本节已撤回旧框架）

本节曾用「白名单 + 当前态小节窗口」框架复述豁免集——该框架已在 ∀ 审计中撤回，
且其列举既**过包含**（列了不再使用的项）又**欠包含**（漏了最大的一项：`历史@` 整行放行）。

**豁免的唯一权威是本文件「检查器现状（SSOT）」节的机制要点表**（三族、逐条列全）。
此处不再复述，只留指针。

### 红基线（先见红，再见绿）

`--baseline` 以**整改前的判据**（只做字节级 C0/DEL 检查）跑同一批定向用例。
**具体数字不在此抄录**（见上节去镜像说明），请运行：

```sh
python3 .harness/changes/2026-07-27-bootstrap-adoption-1/evidence/carrier_sweep.py --baseline
```

红用例全部是「严格可解码、不含 C0/DEL 字节、却不可呈现」的码点族；
同一批用例在当前判据下全绿。这是流程 §3「红测先行」所要求的证据。

### 结果摘要（result digest）

对「定向用例逐条结果（**含输入字节、期望载体与实际载体**）+ 各分支命中数 + 失败/逃逸计数」排序后取 SHA-256。
**其值见本文件「本节保留的唯一两个不变量」小节**（R20 起全库唯一抄本），或当场执行：

```sh
python3 .harness/changes/2026-07-27-bootstrap-adoption-1/evidence/carrier_sweep.py | grep 'result digest'
```

摘要的**检测范围，精确陈述**：它绑定**定向行**的输入字节、期望载体与**实际载体**，
以及穷举层与抽样层的**汇总计数**。因此：定向行的实际载体变化、或任一层计数变化，摘要都会变化。
但它**不绑定未被执行输入的载体**——审阅方污染了 `b"\x00" * 6`（不在定向用例、不在长度 0–2 穷举、
也不在本 seed 的命中集内），实测 `digest_changed=False`。
**发生在从未被执行的输入上的错误，不在本摘要的检测范围内。**
逐条用例表见 `boundary-cases.md`（由 `--emit-markdown` 生成，输入以完整 `repr` 呈现、不截断）。
