# Change Summary — 接入 #1：scaffold 自身接入 Harness

## Status

实现方已完成交付，等待独立审阅。实现方不自评通过、不执行合并（`rules/project.md` §3）。

## Result

本仓库现同时是 Harness 的生产者与消费者：

- 仓库根安装了消费者副本 `.harness/`，并生成三个平台投影 `CLAUDE.md`、`AGENTS.md`、`.cursor/rules/harness.mdc`。**在 `init` 刚完成、尚未经 bootstrap 定制的那一时点**，消费者副本与分发包 `template/.harness/` 的唯一差异是 `init` 写入的 `origin` 印章；该陈述只描述那个时点，不描述本 Change Record 所在的最终状态（最终状态见下方「最终状态的实际差异」）；
- bootstrap 依据代码库侦察 + 所有者访谈裁决，产出项目层资产 `rules/project.md`、`wiki/overview.md`、`wiki/conventions.md`，并注册 `project-rules` 组件；
- CI 追加两条消费者副本门禁，消除「投影静默过期」缺口；
- 分发包 `template/.harness/` 零改动，零内容回灌。

访谈五题的裁决（所有者全部采纳推荐项，Q4 取 A+C 叠加）：Q1 项目层中文、随包英文原样；Q2 门禁 7 条（CI 5 条 + 消费者 2 条）；Q3 本次即把消费者副本纳入 CI；Q4 删除重装的手工同步规则，并把其代价作为 v3 需求证据留档；Q5 项目目的采用起草原文。

### 最终状态的实际差异

本 Change Record 所在 HEAD 上，消费者副本与分发包的真实递归比较：

```text
$ diff -rq template/.harness .harness
Only in .harness/changes: 2026-07-27-bootstrap-adoption-1
Files template/.harness/manifest.json and .harness/manifest.json differ
Only in .harness/rules: project.md
Only in .harness/wiki: conventions.md
Only in .harness/wiki: overview.md
```

即最终差异共三类：

1. `manifest.json` 的 `origin` 印章（`init` 写入）**与** `project-rules` 组件注册（bootstrap 写入）；
2. 项目层目标资产：`rules/project.md`、`wiki/overview.md`、`wiki/conventions.md`；
3. Change Record 审计元数据：`changes/2026-07-27-bootstrap-adoption-1/`。

零回灌（`git diff main -- template/` 为空）证明的是**生产者目录未被消费者内容修改**，它并不意味着消费者副本仍只比分发包多一个 `origin` 印章；两者是不同命题。

## Verification evidence

### 资产提交（本 Change Record 之前已存在，逐个列出，不使用范围表示）

- `c36b7ed37a3f7a772f52a74c49bf328773437f5c` — 安装提交：`init` 产物（消费者副本 `.harness/` 与三个投影）。
- `003829b041ac1e6981912fbdf46070ba239cdccd` — `rules/project.md`、`manifest.json` 的 `project-rules` 注册、三个投影重生成。
- `9c50ed542534c8c91061cd39009c23a417deb3d9` — `wiki/overview.md` 与 `wiki/conventions.md`。
- `2b8100bd60b0000d20a79ae4a9b77b412aeb33a7` — `.github/workflows/validate.yml` 追加两条消费者门禁。
- `8c355041963d03c677940117b9c227ff02a0b8bb` — 依 PR #7 第一轮审阅 F3 的设计方裁定，修订 `rules/project.md` §2 的门禁证据载体条款（命令原文 + 命令自身的终态摘要行 + 退出码）。
- `4651c0297fb0c72dba4eb1805279178c30c2643a` — 依 PR #7 复审 F1 的设计方裁定，将该条款闭合到门禁输出的**全部**情形：(a) 有终态摘要行者记原文、(b) 成功且零输出者逐字记 `<empty>`、(c) 一律禁止人工断言。
- `444cc95812b760ee32a0ad836a0ec3771cd48e7d` — 依 PR #7 第三轮复审的设计方裁定，把该条款改写为**与退出码解耦的输出域三分法**（空 / 非空可解码 / 非空不可解码），互斥穷举由构造保证，并给出「终态摘要行 = 最后一个非空行」的可判定定义；取代上一条的按成功状态划分。
- `f5c3b2a3e962f9f87fdeaa81213bf5014293f89a` — 依 PR #7 第四轮复审的设计方裁定，规定 **stdout / stderr 分流独立捕获与独立记录**（取消「合并输出」概念），并把各分支补成**全函数**：(b) 增设「无非空行 → 字节数 + SHA-256」子分支，写入「行」的字节级定义；另据内部对抗扫描补上「必记行全为控制字节 → 字节数 + SHA-256」例外。
- `9e14d339d7797a3da824c9d4256316da18103f2d` — 依 PR #7 第五轮复审的设计方裁定，把「不可呈现」的判据由字节级 C0/DEL 检查换成**固定枚举的不可呈现码点集**（C0、DEL、C1 含 NEL、U+2028/U+2029、双向控制符、U+FEFF），闭合到流程 §2.3 声明的对抗域，且判定与 Unicode 版本无关。
- `16397a4daffec8119b3d432a40f9448e975b28df` — 依内部 Opus 审阅的设计方裁定，**取消规则对码点集补集的任何全称断言**（集合改述为本项目的固定约定，集合外的不可见码点将被原文记录，属已知且接受的残余风险），并把集合扩充到已知的不可见字符族（`U+00AD`、`U+061C`、`U+200B`–`U+200D`、`U+2060`–`U+2064`、`U+206A`–`U+206F`、`U+FFF9`–`U+FFFB`、`U+FE00`–`U+FE0F`、`U+E0000`–`U+E007F`）；附记行同样受该检查约束。
- `1d8c170bc76e080b2b0f2ecce9a5312c7021724d` — 修正上一条码点表的一处范围错误：原「`U+206A`–`U+206F`（含 `U+2066`–`U+2069`）」一行的括注与范围自相矛盾（隔离符在该范围之下），拆为两行如实列出。
- `4c0a6efce013009126af1f483e738f746ddae9d6` — 依内部复审 Minor B，逐行清理码点表的类别标签，使每个标签恰好命名其范围所含内容（「变体选择符」→「变体选择符（基本区 VS1–VS16）」等）；集合本身不变。

以上十二个提交均不含本 Change Record 的任何审计元数据；资产 diff 即证据，未另做副本。本 Change Record 落在其后的审计提交中。

### 7 条交付门禁

按 `rules/project.md` §2 的证据规则记录。每条门禁的证据 = **退出码** + **stdout 载体** + **stderr 载体**；两个流分别独立捕获、分别判定，不存在合并输出。

分流实测判定表（在本审计提交的父提交上以两个独立管道捕获）：

| 门禁 | exit | stdout 字节 | stdout 分支 | stderr 字节 | stderr 分支 |
| ---: | ---: | ---: | --- | ---: | --- |
| 1 | 0 | 27 | (b) 末非空行 | 0 | (a) `<empty>` |
| 2 | 0 | 27 | (b) 末非空行 | 0 | (a) `<empty>` |
| 3 | 0 | 99 | (b) 末非空行 | 0 | (a) `<empty>` |
| 4 | 0 | 0 | (a) `<empty>` | 23862 | (b) 末非空行 |
| 5 | 0 | 0 | (a) `<empty>` | 0 | (a) `<empty>` |
| 6 | 0 | 27 | (b) 末非空行 | 0 | (a) `<empty>` |
| 7 | 0 | 10 | (b) 末非空行 | 0 | (a) `<empty>` |

**分流暴露出的一个事实**：第 4 条 `unittest` 的全部输出在 **stderr**，stdout 为 0 字节——这是 `unittest` 的既有行为。上一版按「合并输出」记录时，该事实被合并操作掩盖，记录看不出摘要行来自哪个流。这正是分流捕获相对合并捕获的实质收益，不只是消除了拼接顺序的歧义。

无门禁落入 (c)，无门禁落入 (b) 的「无非空行」或「控制字节」子分支。

### 7 条门禁逐条证据

```text
### 1  python3 template/.harness/bin/validate.py
exit=0
stdout: Harness contract is valid.
stderr: <empty>
### 2  python3 template/.harness/bin/harness.py validate
exit=0
stdout: Harness contract is valid.
stderr: <empty>
### 3  python3 template/.harness/bin/harness.py adapt --check --root template/.harness
exit=0
stdout: adapt: ok
        （同流附记：[ADAPT_SKIPPED_TEMPLATE] .: origin is null; template bundles do not generate projections）
stderr: <empty>
### 4  python3 -m unittest discover -s tests -v
exit=0
stdout: <empty>
stderr: OK (skipped=2)
        （同流附记：Ran 175 tests in 12.316s）
### 5  git diff --check e338db8 HEAD
exit=0
stdout: <empty>
stderr: <empty>
### 6  python3 .harness/bin/harness.py validate
exit=0
stdout: Harness contract is valid.
stderr: <empty>
### 7  python3 .harness/bin/harness.py adapt --check
exit=0
stdout: adapt: ok
stderr: <empty>
```

各条 (b) 分支记录的均为该流最后一个非空行的原文；括注为规则允许的同流附记原文行。第 4 条附记的 `Ran` 行含该次运行的动态耗时，第四轮复审已明确接受（规则保存的是该次运行的证据，不承诺未来复跑逐字节相同）。

### 对抗扫描证据（可复现）

扫描证据以文件形式随本 Change Record 交付，位于 `evidence/`。**设计方裁定：Change Record 的
`evidence/` 子目录属该记录的审计元数据**，因此其中文件不进入 `customization-record.md` 的目标资产表；
`carrier_sweep.py` 是本记录的证据材料而非可复用 Harness 组件，故不在 `manifest.json` 注册。

| 文件 | 内容 |
| --- | --- |
| `evidence/carrier_sweep.py` | 载体规则的机械实现 + 扫描器。仅标准库、seed 写死在常量、无时钟输入且不读共享环境状态（argv 路径自检只在自建私有临时目录内操作）、源码纯 ASCII。**默认模式与 `--emit-markdown` 承载同一个认证判定**（0 = 全部 layer PASS，1 = 任一 FAIL；`--emit-markdown PATH` 在失败时**拒绝写入正式文件**，无 PATH 时输出带「不认证」横幅的诊断表）；`--baseline` 是报告模式（预期见红、恒 0、永不写文件）；`-h/--help` 为 0，未知参数 / 互斥组合 / 非法路径为 2。模式矩阵以 `run-manifest.md` 为唯一权威。 |
| `evidence/boundary-cases.md` | 定向用例逐条表（数量以该文件自身为准），**由 `--emit-markdown` 生成并原子写入**，含**期望载体与实际载体两列**，输入与载体均以完整 `repr` 呈现、不截断；表头写明该次运行的自检通过数与随机域失配数，**自带有效性证据**。不得手工编辑。 |
| `evidence/run-manifest.md` | 归属裁定与契约缺口、被验证对象、方法学声明、**交付前 ∀ 句审计表**、**可信计算基披露**、用例来源、算法与参数、复现与再生成命令、结果与红基线、结果摘要。 |

复现命令与结果：

```text
$ python3 .harness/changes/2026-07-27-bootstrap-adoption-1/evidence/carrier_sweep.py
exit=0
stdout: result digest (binds directed rows' actual carriers plus aggregate counts): 20ad0f5005822570f955e0542e7446b21f962b187c630b08d75d5f1d7f4a6b6d
stderr: <empty>
```

上行 `stdout` 载体按 `rules/project.md` §2(b) 记录该流的**最后一个非空行原文**（本次运行实测）。

**变异探针**（复现审阅方的攻击，证明证据会在实现出错时见红）——**数字按去镜像约定不抄录**：
把 `_digest` 打桩为常量后重跑 `run_directed` / `run_fuzz` / `result_digest`，
预期定向失败数与抽样失配数均显著非零、结果摘要改变；整改前同一攻击为 0 且摘要不变。
复现脚本见 `evidence/protocol-runs.md` 的「复核者应执行的命令」。

引入载体值核对之前，同一攻击的结果是 `failures=0`、`digest_unchanged=True`。

**当前态数字按去镜像约定不在此抄录**（第四次复发的教训）：定向用例数与失败数、四类自检项数、穷举与抽样的总数与失配数、分支直方图，一律**运行脚本或读机器生成的 `evidence/boundary-cases.md`**。抽样层的语义不变且不是数字：**抽样只对被抽到的输入成立，未被抽到的输入不在证明范围内**。

结果摘要的**检测范围**：绑定**定向行**的输入字节、期望载体与**实际载体**，以及各层**汇总计数**；**发生在从未被执行的输入上的错误不在其检测范围内**（审阅方 `b"\x00"*6` 探针实测 `digest_changed=False`，与该表述一致）。 历史@e338db8

**红基线**（先见红，再见绿）——数字不抄录，请运行：

```sh
python3 .harness/changes/2026-07-27-bootstrap-adoption-1/evidence/carrier_sweep.py --baseline
```

30 个用例在整改前的判据（只做字节级 C0/DEL 检查）下判错，全部是「严格可解码、不含 C0/DEL 字节、
却不可呈现」的码点族；同一批用例在当前判据下全绿。这是流程 §3「红测先行」所要求的证据。

**方法学更正**（措辞按时点限定）：**早期只检查分支归属、尚无独立 oracle 的旧扫描器**
只能证明「没有输入落到划分之外」（全函数性），**不能证明「输入落进了正确的分支」**——
那是**当时**的局限：NEL / U+2028 / U+2029 这类反例即使被随机生成，也会因「有分支、无异常」
而顺利通过，问题从来不是无处可落，而是**落错了地方**。
**当前的抽样层不同**：它对**每个命中输入**核对**分支与载体值**；
它**不能证明的是未抽中的输入**。因此定向用例逐条携带期望分支，
并从流程 §2.3 声明的对抗域**逐项**取材；B 组还额外包含集合外的**可见邻居**
（U+00A0、U+2010、U+2027、U+202F、U+3000），因为只测集合内成员只能证明「不漏」，
加测邻居才能证明「不误伤」。邻居一律取**已分配且确实可见**的码点——规则对补集不作断言，
拿未分配码点当「应可原文记录」的期望值，等于替规则声称它并未声称的东西。

### 零回灌核对

```text
$ git diff main -- template/ | wc -l
       0
```

### 隔离性

全部生产者契约测试的扫描根固定在 `template/.harness`（`tests/test_template_contract.py:9`、`tests/test_validate.py:14`、`tests/test_harness_cli.py:18`、`tests/test_adapters.py:14`），消费者副本不进入任何扫描域。未对 `tests/` 做任何适配改动。

### v3 `upgrade` 需求证据（本次实测） 历史@e338db8

删除重装路径下，放回 `rules/project.md` 但忘记重新注册组件时，消费者两条门禁**全绿**而投影中已无 `project-rules`（`grep -c project-rules CLAUDE.md` 输出 0）。完整探针与代价分析见 `wiki/conventions.md` §6。

## Exceptions

1. **Skill 契约留白（已上报，待设计方裁定）**：定制记录的 Asset 列要求 bundle 相对路径，但三个平台投影位于分发包之外。本次按「难以归类仍须成行」处理，路径以仓库根相对写出并显式标注。详见 `customization-record.md`。
2. **本次未验证「受管块外用户字节逐字节保留」**：三个投影文件接入前均不存在，v1 该项核心承诺在本次 dogfooding 中没有真实样本。若需该证据，须选一个已有 `CLAUDE.md` 的项目作为接入 #2。
3. **CI 新增步骤的真实结论**须以远端 Actions 运行为准；本地无法证明 GitHub 侧行为。

## 整改记录（PR #7 第一轮独立审阅）

> **本节为该轮历史（历史@ce45a7a7）**：节内现在时均指**当轮时点**，不描述现状；
> 机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

审阅记录：`docs/reviews/2026-07-27-pr-7-scaffold-self-adoption-review.md`，结论 Request changes，绑定 HEAD `ce45a7a7359ab271c878732e0b004c664218fc01`，3 项 Important。设计方裁决后的整改：

| Finding | 裁决与整改 |
| --- | --- |
| F1 定制记录误判 | 接受审阅方。三行投影的 Reusability 由 `generic` 改为 `project`（实际变更内容是本项目的 `project-rules` 注册，而非投影机制的通用性）；Cursor 行 Action 由 `replaced` 改为 `modified`（审阅方实测 26/26 行原文保留、新增 1 行；整文件所有权是写入机制，不等于内容谱系）。 历史@e338db8 |
| F2 init 时点事实误作最终状态 | 「唯一差异为 `origin` 印章」逐字限定到「`init` 刚完成、尚未 bootstrap 定制」的时点，并在本文件新增「最终状态的实际差异」一节列出三类真实差异；PR #7 正文同步修正。 |
| F3 门禁证据载体 | 设计方选审阅方的方案 2：先以资产提交 `8c355041963d03c677940117b9c227ff02a0b8bb` 修订 `rules/project.md` §2 的证据条款，再由本审计提交对齐 summary，消除「规则与首条 Change Record 从第一天起互相矛盾」。 |

本轮整改遵守 Skill 的两段提交契约：规则修订作为资产提交先落，审计提交在其后，且上方资产提交清单已逐个扩充其完整 SHA。

## 整改记录（PR #7 复审 d80ec6a）

> **本节为该轮历史（历史@d80ec6a）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

审阅记录：`docs/reviews/2026-07-27-pr-7-scaffold-self-adoption-rereview-d80ec6a.md`，结论 Request changes，绑定 HEAD `d80ec6a40c14facc25f39af7ab8674e4a05795f2`，2 项 Important，六维总分 20/24。上一轮 3 项仓库内整改经复审确认全部关闭。

| Finding | 裁决与整改 |
| --- | --- |
| F1 静默成功命令无合法证据载体 | 接受审阅方，并按设计方要求做**类修复**而非实例修复：不是给第 5 条门禁单独开一个特例，而是把 §2 的证据条款闭合到门禁输出的**全部**情形——(a) 有终态摘要行、(b) 成功且零输出记 `<empty>`、(c) 禁止人工断言，并声明 (a)(b) 互斥穷举、遇到两者皆不适用者须先补规则不得临场自创。资产提交 `4651c0297fb0c72dba4eb1805279178c30c2643a` 先落，本审计提交随后把第 5 条证据改为 `<empty>` 形式。 |
| F2 PR 正文与仓库记录漂移 | 按审阅方要求订正统计（`7 project / 0 generic / 0 stack`）、恢复第 4 条命令的 `-v`、把「完整输出见 summary」改为按证据载体规则的准确表述。并按设计方的类修复要求，把 PR 正文中**复制自仓库记录的可漂移统计**改写为指向 `customization-record.md@<审计 SHA>` 的指针，正文只保留不可漂移的论断，从结构上消除正文再次与仓库记录矛盾的可能。 |

**本轮的方法学观察**（供流程复盘）：两轮 Important 的根因同属一类——「用存在命题交付本应是全称命题的契约」。第一轮的证据条款只枚举了已知的有输出命令，复审立刻在未枚举的静默命令上找到反例；PR 正文则以复制而非引用的方式重复了可漂移事实，于是每次仓库记录变化都可能产生新的矛盾实例。两次都按「修类不修实例」（[不变式闭包设计法](../../../docs/process/invariant-closure-design.md) §3）处理：前者把规则闭合到输出全域，后者把正文改为指针以消除复制源。

## 整改记录（PR #7 第三轮复审 8a57a1a）

> **本节为该轮历史（历史@8a57a1a）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

审阅记录：`docs/reviews/2026-07-27-pr-7-scaffold-self-adoption-rereview-8a57a1a.md`，结论 Request changes，绑定 HEAD `8a57a1a8bae71c8fb80ba228f4cac3371684dfd5`，1 项 Important，六维总分 21/24。上一轮 F2（PR 正文指针化）经复审明确接受，不再要求把统计内联回正文；Spec 一路已判 Approve。

| Finding | 裁决与整改 |
| --- | --- |
| 证据规则的全称范围自我否定 | 设计方裁定审阅方的方案 2，两个子论点均成立：其一，规则一边宣称 (a)(b) 互斥穷举、一边在同段承认可能存在两者皆不适用的门禁，文本自我否定；其二，用**成功状态**作划分维度是错的——非零退出且零输出既无摘要行也不满足「成功且零输出」，落在两支之外。整改：把划分维度换成**输出本身**，沿「空 / 非空」与非空时「可否 UTF-8 严格解码」两个构造性维度三分，穷举性由构造保证而非由声称保证；退出码升为每条门禁独立必记字段，成功与否只由它表达；并把「终态摘要行」定义为**最后一个非空行**，消除「哪一行算摘要」的人工判断。资产提交 `444cc95812b760ee32a0ad836a0ec3771cd48e7d` 先落，本审计提交随后对齐。 |

**本轮的方法学观察**：这是同一根因的第三次实例，且这次的教训比前两次更深一层——前两轮的问题是「枚举不全」，本轮的问题是**划分维度选错**。按成功状态划分时，无论怎样补充分支都无法穷举，因为该维度与「输出长什么样」正交；换成输出自身的构造性维度后，穷举性不再需要声称，它由划分方式本身产生。可machine化的判据（0 字节 / 严格解码 / 最后一个非空行）同时消除了执行者的自由裁量。这条经验适用于任何「声称覆盖全域」的自然语言契约，建议纳入流程复盘。 历史@8a57a1a

## 整改记录（PR #7 第四轮复审 0bcbbc8）

> **本节为该轮历史（历史@0bcbbc8）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

审阅记录：`docs/reviews/2026-07-27-pr-7-scaffold-self-adoption-rereview-0bcbbc8.md`，结论 Request changes，绑定 HEAD `0bcbbc8c410c655998d2720c0f7861e1e696c0ae`，Standards / Spec 各 1 项 Important，六维总分 20/24。Gate 4 动态耗时经复审明确接受，不构成问题。

| Finding | 裁决与整改 |
| --- | --- |
| (b) 分支存在「无非空行」的未定义子域（最小反例 `b"\n"`） | 接受。分类穷举不等于**记录义务闭合**：分类能选中 (b)，但该分支要求记录的对象可以不存在。整改把 (b) 内部沿「是否存在非空行」二分，无非空行者记字节数 + SHA-256，使每个分支要求记录的对象必然存在；并写入「行」的字节级定义（仅 `0x0A` 为分隔符，去除末尾一个 `0x0D`），消除对「行」的临场解释。 |
| stdout / stderr 的「合并输出」无唯一捕获语义 | 接受审阅方案 2。两个流**分别独立捕获、分别记录**，规则中不再存在「合并输出」这一对象，拼接顺序、运行时交错、多字节序列跨流分布三类歧义因此不可能发生——不是规定了一种合并方式，而是取消了合并这一步。 |

资产提交 `f5c3b2a3e962f9f87fdeaa81213bf5014293f89a` 先落，本审计提交随后对齐。

**本轮的方法学观察**：前三轮的教训依次是「枚举不全」→「划分维度选错」→ 本轮的**两个新层次**。其一，*分类穷举 ≠ 记录义务闭合*：一个全函数不仅要求每个输入落入某分支，还要求该分支的输出确实存在；只检查前者会漏掉后者。其二，*规则的输入本身必须先被唯一确定*：在输入字节串未定义之前，任何关于它的判定规则都不可能是良定义的——三分法再完美，喂给它的东西不唯一，结论就不唯一。取消合并（而非规定合并顺序）是更强的修法：它消灭了不确定性的来源，而不是给不确定性挑一个约定。

**内部对抗扫描**（本轮起作为交付前纪律，见下）另发现并关闭第三个缝隙：必记行可以整行由控制字节构成（`b"\r\r\n"` 的最后一个非空行是单个 `0x0D`），无法在 Markdown 记录中无损呈现，强求「记录原文」会逼出规则自身禁止的临场转义记法；该子域已并入字节数 + SHA-256 载体。

## 整改记录（PR #7 第五轮复审 3a2ccbf）

> **本节为该轮历史（历史@3a2ccbf）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

审阅记录：`docs/reviews/2026-07-27-pr-7-scaffold-self-adoption-rereview-3a2ccbf.md`，结论 Request changes，Standards 3 项 / Spec 2 项 Important（3 个不同问题），六维总分 18/24。上一轮的分流捕获、字节级行定义、无非空行 fallback、两段提交与逐 SHA 均经复审通过。

| Finding | 裁决与整改 |
| --- | --- |
| 对抗扫描无可复现载体（断言式交付） | 接受。扫描本身真实执行过，但证据留在被 git 忽略的临时目录里，交付物中只剩一句「已扫描」——这正是本项目强制流程明令禁止的断言式交付，且违反的是我们自己写进 `wiki/conventions.md` 的条款。整改：扫描器、逐条用例表与运行清单全部落入 `evidence/`，随记录一同交付并可独立复现。 |
| PR 正文仍描述整改前的载体语义（第三次漂移） | 接受，并采用**完整类修复**：正文中关于载体语义的叙述**全部删除**，门禁一节只保留不可漂移的陈述（每条门禁记 exit + stdout 载体 + stderr 载体），分支与判定细节一律以 `rules/project.md@<资产 SHA>` 与 `summary.md@<审计 SHA>` 为准。前两轮只订正了当时错误的那句话，所以第三次仍会漂移；这次删除的是**正文持有载体语义副本**这件事本身。 |
| 控制字节 fallback 未覆盖强制对抗域的 Unicode 控制/分隔符 | 接受。字节级 C0/DEL 检查漏掉了 NEL、U+2028、U+2029 与双向控制符——它们严格可解码、不含 C0/DEL 字节，却同样不可无损呈现。整改采用**固定枚举的不可呈现码点集**而非 Unicode 类别判定：类别表随 Unicode 版本变动，枚举则版本无关；代价（新版 Unicode 引入新控制字符需回规则补条目）已写进规则本身。 |

**本轮的方法学观察**：前四轮的教训是规则文本本身的缺陷（枚举不全 → 划分维度错 → 记录义务未闭合 + 输入未定义）。本轮暴露的是**验证方法**的缺陷：我用随机模糊测试去验证一个**判据**，而随机测试在原理上无法验证判据正确性。规则第五次被推翻，不是因为第五次写得更差，而是因为前四次的验证手段从一开始就选错了工具。这条教训比任何一条具体规则都更值得沉淀。

## 整改记录（内部 Opus 独立审阅，badccea9）

> **本节为该轮历史（历史@badccea9）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

owner 指示本轮先走**内部**独立审阅再交外部审阅方。内部审阅在 `badccea9` 上给出 5 项 Important + 3 项 Minor，均经设计方裁定为真。 历史@badccea9

| Finding | 裁决与整改 |
| --- | --- |
| 规则仍对码点集**补集**作全称断言 | 接受，并按设计方裁定**终结这场域军备竞赛**：规则不再对补集作任何断言。集合改述为「本项目的固定约定（可判定、版本无关）」，并明写残余风险——集合外若存在其他不可见/格式码点，将被按原文记录，这是已知且被接受的，不是规则声称已排除的。此前每一轮都在为补集断言找反例，根因是那句断言本身不可能为真：固定枚举撑不起一个关于全体 Unicode 的全称命题。 |
| 集合遗漏已知的不可见字符族 | 接受。补入 `U+00AD`、`U+061C`、`U+200B`–`U+200D`、`U+2060`–`U+2064`、`U+206A`–`U+206F`、`U+FFF9`–`U+FFFB`、`U+FE00`–`U+FE0F`、`U+E0000`–`U+E007F`，仍为固定枚举、逐条列出、版本无关。 |
| `boundary-cases.md` 的来源不可信 | 接受。该表此前由一次性脚本生成且输入列被截断，无法核对。现由 `carrier_sweep.py --emit-markdown` **真正生成**，输入以完整 `repr` 呈现不截断，文件头写明「不得手工编辑」，`run-manifest.md` 给出精确再生成命令；结果摘要扩展为**绑定输入字节列**，改输入而不重跑即摘要不匹配。 |
| 邻居用例取了未分配码点 | 接受。原邻居含 `U+2065`（未分配）与 `U+206A`（现已入集）。未分配码点不能充当「应可原文记录」的期望值——规则对补集不作断言，那样做等于替规则声称它并未声称的东西。改用已分配且确实可见/可见占位的邻居：`U+00A0`、`U+2010`、`U+2027`、`U+202F`、`U+3000`。 |
| `evidence/` 的审计元数据身份缺乏裁定 | 接受。本记录明载**设计方裁定**：Change Record 的 `evidence/` 子目录属该记录的审计元数据。同时如实上报这是**第二处 Skill 契约缺口**——Skill 的排除清单只列 `summary.md`/`spec.md`/`tasks.md`/`customization-record.md`/`originals/`，未含 `evidence/`；与「投影文件无 bundle 相对路径」并列，留待 B0 成熟化波次统一修 Skill。 |
| Minor：附记行未受不可呈现检查约束 | 已修：附记行同样受该检查；含集合内码点的行不得原文附记，直接略去即可（附记本就是可选的）。 |
| Minor：缺红基线 | 已修：新增 `--baseline`（整改前判据），实测 **当轮历史值 69** 个用例中 **30 个见红**，同一批在该轮判据下全绿（定向用例数与红基线数见 机器生成的 `boundary-cases.md`）。 历史@badccea9 |
| Minor：PR 正文措辞 | 已修：「不复述任何载体语义」改为「不复述具体分支与判定细节」。 |

**本轮的方法学观察**：外部审阅五轮都在攻击「补集里还有没有别的不可见字符」，而真正的病灶是规则**为什么要对补集说话**。内部审阅指出这一点后，正确的修法不是再补一批码点，而是**撤回那个断言**——把「我已排除全部不可见字符」换成「我处理这些，其余是已知残余风险」。前者每加一个反例就被推翻一次，后者对任何反例都成立。**当一个命题反复被证伪时，值得怀疑的不是证据收集得够不够，而是这个命题本不该被断言。**

## 整改记录（内部 Opus 复审，149805a）

> **本节为该轮历史（历史@149805a）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

内部复审确认前 8 项全部关闭、判定「可交外部审阅」，同时新发现 2 项 Minor（事实准确性类）。本微轮次关闭之：

| Finding | 整改 |
| --- | --- |
| Minor A：`run-manifest.md` 的退出码陈述对两个模式为假 | 两侧同时修。脚本侧：退出码语义**按模式**明确——默认模式承载判定（失败或逃逸则非 0）；`--baseline` 与 `--emit-markdown` 为**报告模式**，退出码只表示运行完成（正常 0），因为基线**预期见红**，以失败数决定退出码会让「如期见红」被误读成「扫描失败」；未知参数拒绝执行、打印用法行、退出 2。文档侧：`run-manifest.md` 的退出码一节改为逐模式表格，并明写「不要用报告模式的退出码判断规则是否成立」。 |
| Minor B：类别标签覆盖面大于其范围 | 逐行清理标签，使其恰好命名范围所含内容：「变体选择符」→「变体选择符（基本区 VS1–VS16）」（VS17–VS256 在 `U+E0100`–`U+E01EF`，不在本行）、「零宽字符」→ 其实际列出的三个、「双向标记」→ LRM/RLM、嵌入/覆盖行补上其含有的「弹出」。**集合本身未扩充**——集合外由残余风险条款覆盖，这是设计选择，不是遗漏。 |

两项同属一类：**标签或文档所声称的覆盖面大于其实际内容**，与此前的隔离符范围错误、以及规则曾对补集作全称断言是同一个毛病的不同尺度。修法一致：让陈述缩回到它真正成立的范围。

## 整改记录（外部复审 140b65bf）

> **本节为该轮历史（历史@140b65bf）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

审阅记录：`docs/reviews/2026-07-27-pr-7-scaffold-self-adoption-rereview-140b65bf.md`，结论 Request changes，Standards 1 项 / Spec 0 项（Spec 一路 Approve），根审阅者追加的变异探针揭示了 Spec 路径未识别的证据完整性缺口，六维总分 19/24。

| Finding | 裁决与整改 |
| --- | --- |
| 定向扫描只核对分支，不核对载体内容 | 接受，**变异探针是决定性的**：审阅方把 `_digest()` 打桩成常量 `bytes=0 sha256=000…0`，47 行载体全错，而扫描仍报 0 失败、digest 一字不变。这说明此前的证据只能证明「输入落入预期分支」，证明不了「该分支记录了正确的值」，而后者才是载体规则的一半内容。整改按审阅方五项要求：①每个定向用例同时携带期望分支与期望载体；②期望载体**独立构造**——原文期望是手写字面串，哈希期望由**字面字节数** + 直接 `hashlib.sha256()` 组成，`_digest()`/`carrier()` 一次都不参与；③通过条件为分支与载体**双双**比中；④生成表新增期望载体与实际载体两列，完整不截断；⑤默认模式内置**比较器自检**（错误 SHA / 错误字节数 / 错误原文末行 / 错误分支四项变异必须被拒，外加一项对照必须被接受），任一未达预期即非零退出。同一变异探针在该轮的历史结果（绑定当轮 HEAD `a402986`）：`failures=47`、`digest_unchanged=False`。 |
| 两个互斥报告模式可同时启用并产出自相矛盾的正式证据 | 接受。此前参数解析只拒绝未知参数，`--baseline --emit-markdown` 可同时生效，把 baseline 的失败与现行判据的 fuzz 计数混进同一个 digest，还能按文档给出的重定向方式静默覆盖正式 `boundary-cases.md`，结论句同时写着「All … expected branch」和 `failures: 30`。整改：组合被拒绝（两种顺序均退出 2 并打印用法），argv 自检覆盖该回归，`emit_markdown()` 的结论句按 `failures` 如实分支——失败非零时明写「本表不认证该规则」并逐条列出失败用例。 历史@140b65bf |

**关于提交计数的披露偏差**：审阅方指出自 `3a2ccbf3` 起实际有 7 个提交，而我此前口头汇报为 6 个。该错误未进入仓库或 PR 正文（两处的逐 SHA 枚举一直正确且完整），但确实是一次不准确的披露，在此记录。

**本轮的方法学观察**：前几轮我把「规则被正确分类」当成了「规则被正确执行」。分类是判据的一半，**记录什么值是另一半**，而我的验证只覆盖了前一半——于是一个分类完全正确、记录全是垃圾的实现可以拿到满分绿。变异探针之所以决定性，是因为它直接攻击**验证手段本身**而非被验证对象：它问的不是「规则对不对」，而是「如果实现是错的，你的证据会不会变红」。答案曾经是「不会」。**验证手段必须先证明自己会在实现出错时见红，否则它的绿色不携带信息。** 比较器自检与自检对照就是把这个要求固化进脚本。

## 整改记录（内部变异攻击复审，a402986b）

> **本节为该轮历史（历史@a402986b）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

内部复审以变异攻击 brief 复核：值验证层顶住 11 类变异（含 4 类我未列出的），判定 Ready，但提出 1 项 Important + 2 项 Minor + 1 项 nit，本轮先行关闭。 历史@a402986b

| Finding | 裁决与整改 |
| --- | --- |
| Important A：摘要「能检出错误载体」是全称陈述，实际只在定向域为真（M10） | 接受，并按设计方裁定取**强解**——不是把句子缩小到定向域，而是**把陈述做成真的**。M10 只污染 69 个定向输入**之外**的载体值：定向用例逐条仍全绿，而摘要绑定的正是定向行，digest 一字不变。整改给随机域加**独立值 oracle**：`oracle_carrier()` 是同一规则的**第二次独立转写**，不调用 `carrier()`/`_digest()`，手工遍历字节而非 `split()`，哈希由 `hashlib` 直接重建；`run_fuzz()` 对**每个**随机输入比对分支与载体值，两次转写在任意输入上分歧即计入失配并使运行见红。M10 前后：整改前随机域根本不检查载体值、digest 不变（绿）→ 整改后 `fuzz_mismatches=170981`、digest 改变（红），而定向 `failures` 仍为 0，说明抓住 M10 的正是随机域 oracle。 |
| Minor B：生成表在比较器被污染时不变 | 两项自检现在也在 emit 模式运行，通过数与随机域失配数写进表头，产物**自带有效性证据**。实测比较器污染确实改变表头：恒真 → `1/5`，恒假 → `4/5`，忽略载体半边 → `2/5`（正常为 `5/5`）。 历史@a402986b |
| Minor C：文档给出的再生成命令会在中途失败时截断正式表 | shell 重定向一打开文件就截断，运行失败会在 Change Record 里留下零长度或半截的正式表。改为脚本自己落盘：`--emit-markdown PATH` 用同目录临时文件 + `os.replace()` **原子写入**（与本仓库 CLI 既有的写入模式一致），旧表在完整新表就绪前不受影响；无 PATH 时仍流向 stdout 以便 diff。 |
| nit：`-h`/`--help` | 用法打印到 **stdout**，退出 **0**。argv 自检扩充至 15 项，覆盖带路径、多路径、路径缺 flag、`--baseline` 带路径与两种 help 形式。 |

**本轮的方法学观察**：上一轮我刚写下「验证手段必须先证明自己会在实现出错时见红」，
而 M10 说明我只在**定向域**证明了这一点，却把结论当成了全域成立——**同一个「全称陈述只在子域为真」的毛病，在证据层原样重演了一次**。
更值得记的是修法的选择：把句子缩小到「仅定向域」也能消除矛盾，但那样只是让陈述变弱；
给随机域配独立 oracle 才是让原来的强陈述**成为真的**。
可以缩小陈述，也可以做大实现——判断依据是那个强陈述本身是否值得成立，而不是哪种改法省事。

## 整改记录（外部复审 50a6060）

> **本节为该轮历史（历史@50a6060）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

审阅记录：`docs/reviews/2026-07-27-pr-7-scaffold-self-adoption-rereview-50a6060.md`，结论 Request changes，Standards 2 项 / Spec 2 项 Important（合并为 3 个不同问题）+ 2 项 Minor，六维总分 17/24。

| Finding | 裁决与整改 |
| --- | --- |
| 期望与 oracle 仍与被测代码共享判定数据 | 接受。此前「独立」只做到「不调用 `carrier()`/`_digest()`」，但双方仍读同一个 `CARRIER_EMPTY` 与同一个 `UNRENDERABLE_SET`——审阅方两个探针都全绿：把 `CARRIER_EMPTY` 改成 `"<wrong>"`、或删掉未被定向覆盖的 U+0001，被测实现已违反规则而检查毫无反应。整改：①空流期望改用独立字面量 `EXPECTED_EMPTY_LITERAL`；②oracle 拥有**自己的第二份**码点集字面列表，导入时交叉断言逐元素相同，单侧改动立即报错；③新增 5 项**共因自检**（错误空载体、单侧删除未覆盖成员、被删成员确实会改变行为，外加两项对照）；④`run-manifest.md` 新增**可信计算基披露**，逐项列出仍然共享的东西（Python 运行时、`hashlib`、UTF-8 解码器、两份集合的交叉断言、以及**同一人对规则文本的两次转写**），独立性声明只在该披露之外成立。实测：两个探针现在分别 **exit 1**（自检失败）与 **exit 1**（导入断言）。 历史@50a6060 |
| help 模式绕过 argv 校验 | 接受。`parse_args()` 见到 `-h`/`--help` 即提前返回，早于未知参数、互斥模式与路径校验，于是 `--help --bogus`、`--help --baseline --emit-markdown`、`-h stray.md` 全部 exit 0——**新增的 help 入口把上一轮刚声称闭合的参数域重新打开了**。整改：先校验完整 argv 再选择模式；help 必须是 argv 中的**唯一** token（`-h --help` 亦拒绝）；argv 自检由 15 项扩至 **22 项**，覆盖 help × 未知参数 / × 各模式 / × 路径 / × 三元组合 / × 顺序颠倒。 |
| 20 万抽样被写成对长度 0–6 的全域检测能力 | 接受，**两手并用**。(a) 在负担得起的地方给出真正的全称证明：新增**长度 0–2 全域穷举**（65,793 个输入全部与 oracle 比对，失配 0）；(b) 对长度 3–6 **撤回**全域措辞，精确表述为「固定 seed 的 200,000 次抽取、**137,527 个唯一输入**，每个被抽到的输入都经 oracle 比对；**未被抽到的输入不在证明范围内**」。全域共 282,578,800,148,737 个输入，不试图穷举。所有文档（manifest、生成表结论、summary）统一采用分层限定表述。 历史@50a6060 |
| Minor：生成表仍称 fuzz「只能证明没有输入逃出划分」 | 已改为分层表述：定向 = 钉住边界的独立期望；穷举 0–2 = 该子域上的全称结论；**抽样层对每个命中输入做独立 oracle 分支 + 载体核对；不能证明的是未抽中输入**。 |
| Minor：依赖清单遗漏 `os` / `tempfile` | 已补齐。 |

**本轮的方法学观察**：三项 Finding 是同一个物种的三次现身——**一个 ∀ 陈述，其声称的论域大于它真正成立的论域**。「期望是独立的」（真论域：不含共享常量与共享集合）、「参数域已闭合」（真论域：不含 help 组合）、「随机域上普遍成立」（真论域：**当轮**被抽到的那些输入；具体数字属 R7 轮历史记述）。十五轮 finding 回头看几乎全是这一个物种。

因此本轮起引入**交付前 ∀ 句审计**作为常设义务：把改动产物中每一句全称陈述逐条列出，标注它是「由构造保证」「已限定到真论域」还是「已撤回」，并随交付提交该审计表。这不是又一条规则，而是把「陈述的论域是否等于其真论域」变成每次交付都必须机械回答的问题——前面十五轮的返工，正是因为这个问题从来没有被系统地问过。

## 整改记录（内部定向审阅，002b5d21）

> **本节为该轮历史（历史@002b5d21）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

内部审阅对 ∀ 句审计表逐行做**针对性证伪**：12 行中 **11 行顶住**；第 7 行（长度 0–2 全域穷举）
捕获了一个对其他所有层都不可见的投毒——**该工具本身有效**。两项待closed，本轮关闭。

| Finding | 裁决与整改 |
| --- | --- |
| Important A：可信计算基披露**自身不完整**，违反了它自己的精确性声明 | 接受。披露表列了运行时、`hashlib`、解码器、两份集合，却**漏了五个 `BRANCH_*` 标签常量**——它们同时被 `carrier()`、`oracle_carrier()` 与全部定向期望读取。S2 变异把其中两个标签改成同一个字符串，五分划分**静默塌缩为四分**，全绿通过。整改按设计方偏好取**对称形式**：检查侧拥有自己的 `ORACLE_BRANCH_*` 字面量（定向期望亦改用之），导入时**成对交叉断言** **且** **各自唯一性检查**——两者缺一不可，因为标签碰撞不会让任何一对不相等。新增 3 项共因自检（碰撞检出、单侧改名检出、对照）。实测 S1、S2 现在均在导入时 `AssertionError`、**exit 1**。诚实记录缓解项：S1/S2 确实会改变 digest，但那是**事后可察觉**，不能代替**事前拒绝**，故不作为修复计入。 历史@002b5d21 |
| Minor B：`--emit-markdown ""` 被接受后以 `FileNotFoundError` 栈回溯死亡 | 接受。它以 **exit 1** 结束，而 exit 1 是留给「规则有缺陷」的，乍看无法与真正的失败区分。现在空路径与纯空白路径在参数校验阶段即拒绝，**exit 2 + usage**；argv 自检 22 → **24 项**。 历史@002b5d21 |

**前瞻性说明**（已写入 `run-manifest.md`）：参数域闭合**只认证已交付的 argv 空间**；
新增任何 flag 都必须同时扩充 argv 自检，否则旧的闭合结论对新空间不再成立。

**∀ 句审计表随之更新**：第 6 行「参数域已闭合」由**构造保证**下调为**已限定**——
上一轮宣布闭合后，新增的 help 入口立刻使其失效，这是把它降级的实证理由；
新增第 5b 行「五个分支标签互异且两侧一致」（构造保证）；
第 4 行「期望值独立」的限定语补入分支标签与「双侧同改」残余风险。

**本轮的方法学观察**：Important A 的形状值得单记——**披露表本身也是一个 ∀ 陈述**
（「这里列出了全部仍被共享的东西」），而它同样犯了「声称的论域大于真论域」的老毛病。
连**为了诚实而写的那张表**都需要被审计，说明 ∀ 句审计不能只覆盖「结论性陈述」，
必须覆盖**一切带全称语气的句子**，包括元层面的披露与免责。

## 整改记录（外部复审 4d698f98）

> **本节为该轮历史（历史@4d698f98）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

审阅记录：`docs/reviews/2026-07-28-pr-7-scaffold-self-adoption-rereview-4d698f98.md`，结论 Request changes，2 项 Important + 2 项 Minor，六维总分 18/24。该记录同时给出一份**可复现审阅协议**，本轮已按其第 5、6 步自验。 历史@4d698f98

| Finding | 裁决与整改 |
| --- | --- |
| I1：穷举层已失败时，生成模式仍 exit 0 并输出相反的认证结论 | 接受。根因是**认证状态有两个**：`emit_markdown()` 只消费 69 个定向用例的 `failures`，而自检、穷举、抽样三层的结果它看不见——于是污染 `carrier(b"\x00\x00")`（无定向用例覆盖，但穷举必然访问）产出的正式表同时写着「1 mismatches」与「EVERY input agrees」，exit 0，还能原子覆盖仓库内正式证据。整改：定义**唯一的认证状态** `certification_state()`，是 directed / 比较器 / 共因 / argv / 穷举 / 抽样(含 undefined) **六层的合取**；任一层失败时 `--emit-markdown PATH` **拒绝写入正式文件并 exit 1**，诊断能力只保留在「无 PATH → stdout + 不认证横幅 + exit 1」这条**不共享成功语义、不共享正式路径**的通道上；结论块**逐层如实分支**，穷举失败时那句无条件的「EVERY input ... agrees」不再出现。新增**认证自检 9 项**（该轮历史值，绑定当轮 HEAD `d3290cd`；项数现读 机器生成的 `boundary-cases.md`），逐层证明失败会同时翻转结论文本与退出语义。 |
| I2：∀ 审计并未枚举「每一句」，且仍遗漏可证伪的全称陈述 | 接受，且**审计自身的完成度声明也被收窄**。(a) digest 检测范围：改为「绑定定向行的实际载体 + 各层汇总计数；**未被执行输入上的错误不在检测范围内**」，与审阅方 `b"\x00"*6` 探针实测 `digest_changed=False` 一致；(b) 确定性：收窄为「**本环境实测 `Python 3.14.5`** + 固定 seed 下逐位可重现，**不对跨 Python 版本作承诺**」，并引用 CPython 关于 random 跨版本可复现性的官方说明——按设计方裁定**不自造 PRNG**（更多机械装置正是此处的反模式）；(c)「A corrupted comparator changes these counts」限定为自检枚举的变异类。∀ 审计以审阅方给出的 `rg` 候选收集命令为**机械枚举基础**（本轮命中 175 行，7 个文件），逐条人工按其五分法分类，新增第 13–17 行；**审计自身的完成度声明限定为「收集器命中集 + 人工分类」**，并显式列出未覆盖之处（正则不含语义分析、人工分类不可机械消除）。 历史@4d698f98 |
| Minor 1：抽样层能力表述与实现矛盾 | 已更正为「对每个命中输入做独立 oracle 的分支 + 载体核对；不能证明的是**未抽中**的输入」，并作为第 16 行进入审计表。 |
| Minor 2：默认判定语义记录滞后（「双自检 / 两项」） | 已在脚本、manifest、summary 三处同步为「四类自检 + 穷举层」。 |

**本轮的方法学观察**：I1 的形状是**一个系统里存在两个「成功」定义**——退出码消费全部层，而正式产物只消费其中一层。只要两个定义不同源，产物就能在系统自认为失败时宣称通过。修法不是让产物「也检查一下别的层」，而是把认证收敛成**唯一状态**，让所有出口（退出码、结论文本、是否落盘）都从它派生。

I2 则再次印证：**为收窄声称而写的东西，本身也会过度声称**。上一轮的 ∀ 审计写着「逐条列出每一句」——这句话自己就是未经检验的全称句，审阅方当场找出两条漏网。本轮起，审计的完成度声明被限定为「收集器命中集 + 人工分类」，并附「未覆盖与已知不足」。**能被诚实声明的，只有实际做过的那件事。**

## 整改记录（三会话协议自验，d3290cdd）

> **本节为该轮历史（历史@d3290cdd）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

会话 A（Standards）与会话 B（Spec）在**彼此不可见**的上下文中各自执行审阅协议，**双双 request-changes**，且给出**重合的反例**。会话 C 将在本轮整改后执行完整机械协议。

| Finding | 裁决与整改 |
| --- | --- |
| **R1 [Critical，A-C1 = B-I1]** 认证合取按「类」形状漏层 | 接受。`certification_state()` 用**三个硬编码前缀**分桶自检失败，`"certification:"` 不匹配任何一个而被**静默丢弃**——认证自检失败时仍得 `certified: YES`、exit 0，并把正式表覆盖为 CERTIFIED；两个会话各自独立执行到了这一步。同时 `CERT_LAYERS` 是**死代码**（A-M3），层集合与分桶集合可以各自漂移。整改按 B 的处方：`CHECK_CLASSES` 成为**唯一来源**，`CERT_LAYERS` 由其派生并被实际消费（不一致即 `AssertionError`）；新增**兜底层「未注册自检失败」**——前缀未注册的失败标签一律 **fail closed**。两条回归实测：认证自检失败 → `default/stdout/path 全部 exit 1`、拒绝写入、出现「does NOT certify」；注入未注册前缀 → 同样 fail closed。 |
| **R2 [Critical，A-C2]** 陈旧的实测值 | 接受。脚本 SHA-256 已刷新（**当轮历史值** `32d77cf155cde9b705d0158dc2920cf1e284afc78553b00c5a862c0c1c0727ef`；当前值见 `run-manifest.md` 参数表）；门禁载体行改为按 `rules/project.md` §2(b) 记录**本次运行**该流的最后一个非空行原文；`_digest` 打桩探针的两个 digest **在当前脚本上重跑并重新记录**（未变异 `837bab63…`；打桩后 `299e7d2e…`，`directed_failures=47`、`sampled_mismatches=151811`、`digest_changed=True`）。 历史@d3290cdd |
| **R3 [Important，A-I1 = B-I2]** ∀ 审计枚举基不是同一状态 | 接受。上一版记录的 175 是**跨状态混合值**（B 证明：该 HEAD 191、前一状态 157，记录的分布两者都不匹配）。现改为**提交前最后一步**在最终内容上重跑收集器，并迭代到不动点：命中 **360 行**，分布逐文件记录；新增第 18–24 行覆盖本轮出现的承载能力声明；**删除**「其余命中不构成全称能力声明」这句概括兜底——它本身就是又一个未经逐条检验的全称句；表基说明改为「**基于最终提交态的收集器命中集 + 人工分类**」。 历史@d3290cdd |
| **R4 [Important，B-I3]** digest 收窄未落到全部位置 | 接受。`summary.md` 的两处均已改为「绑定定向行的实际载体 + 各层汇总计数；未被执行输入上的错误不在检测范围内」；`tasks.md` 的「三处同步」已按真实计数订正并说明生成表表头另有新增行。 |
| **R5 [Important，A-I2]** 认证自检缺机器载体 | 接受。生成表的**层状态表**与**表头 attestation** 现含 `certification self-test` 行与其计数（该轮历史值 12/12，绑定当轮 HEAD `d3290cd`；实测值现读 机器生成的 `boundary-cases.md`），`run-manifest.md` 结果表亦增该行——不再有仅靠人写的断言（`rules/project.md` §2(c)）。 历史@d3290cdd |
| **R6 Minors** | USAGE 重写：列出**全部层**（含穷举）、`--emit-markdown` 明确标注**承载判定**、原子写入语义更新；`_selftest_certification` 的硬编码 `failures=0` 改为**显式标注的 fixture**（含 `FIXTURE-DIGEST`）；docstring 计数订正为**十二项**；`tasks.md` 历史条目中的「20 万」标注为**当轮历史措辞**；层数表述在 summary 各处统一为「四类自检 + 穷举层」（该轮历史值 5/8/24/12，绑定当轮 HEAD `8a57a1a`；数值现读 机器生成的 `boundary-cases.md`）。 |

**本轮的方法学观察**：R1 是**形状层面**的漏洞，不是数值层面的。合取覆盖了「我记得的那些类」，
而不是「系统里存在的那些类」——于是新增一类检查时，它的失败落进了没有人接收的缝隙里。
把层集合改为**从注册表派生**并加**兜底层**之后，遗漏的后果从「静默通过」变成「fail closed」。
**当一个合取的项是手写枚举时，它保护的是作者的记忆，不是系统的状态。**

R3 则暴露了一个更朴素的错误：我在**编辑中途**运行收集器，随后又改了文本，于是记录的数字
不对应任何一个真实状态。证据必须在**它所描述的那个状态上**采集——这与「结论只绑定精确 HEAD」
是同一条纪律，只是尺度更小。

## 整改记录（会话 C 机械协议校验，e7f3d722）

> **本节为该轮历史（历史@e7f3d722）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

会话 C 独立执行完整机械协议：确认 A/B 的全部发现（我的修复在旧 HEAD 上复现见红、在新 HEAD 上闭合），六维 **22/24**；另抓出 4 项，本微轮次关闭。

| Finding | 裁决与整改 |
| --- | --- |
| **NC-1**（必修）收集器计数在两处仍为旧值 | 接受。`summary.md` 与 `tasks.md` 记的是 204，而 HEAD 的真实不动点是 208（`run-manifest.md` 正确）。根因是这两个文件**本身就在收集器的命中集内**，改它们会改变计数。已订正数字，并在**所有内容编辑完成之后**重跑收集器迭代至不动点，确认三处记录值与最终实测值一致。 历史@e7f3d722 |
| **NC-2** R1 的对称一半：注册但无产出的类 | 接受。注册表只解决了「有失败但无层接收」，没解决「有层但无检查执行」——注册一个没有产出者的类会得到伪造的「0 failures PASS」行与 `certified: YES`（会话 C 实测）。整改：`CHECK_CLASSES` 每项**携带其产出者**，`run_check_classes()` 按注册表驱动执行并统计**实际执行的检查数**；某注册类执行数为 0 时**fail closed**（层详情写明 `NO CHECKS RAN`）。回归：为每个注册前缀各加一项「执行数为 0 即 fail closed」，另用真实注入验证——注册一个无产出者的 `orphan` 类，`default` 与 `--emit-markdown PATH` 均 **exit 1**，正式文件未被改写。 历史@e7f3d722 |
| **NC-3** 表头 attestation 硬编码四个类名 | 接受。attestation 现由 `attestation_lines()` **从注册表派生**，与 `CERT_LAYERS` 同一单一来源规则；新增回归：每个注册类必须**同时**出现在层状态表与 attestation 中。 历史@e7f3d722 |
| **NC-4** 自检只断言标记存在，删掉横幅仍全绿 | 接受。新增两项断言：未认证输出的**第一行**必须是横幅首行（与 USAGE 的承诺一致），已认证输出的第一行必须不是。实测删除 `out.append(UNCERTIFIED_BANNER)` 后该项 **FAIL** 且默认模式 **exit 1**。 历史@e7f3d722 |

**本轮的方法学观察**：NC-2 是 R1 的**对称补集**。R1 修好了「失败无处可去」， 历史@e7f3d722
却留下了镜像缺口「层无检查可依」——同一个注册表，一个方向做了保护，另一个方向没有。
**一个双向关系只保护了一个方向时，另一个方向就是下一个缺陷的位置。**

NC-1 则再次印证 R3 的教训还不够彻底：我把收集器移到了「提交前最后一步」，
却在此后又编辑了处在其命中集内的 summary 与 tasks。**不动点必须在最后一次内容编辑之后取，
而记录数字这件事本身就是一次内容编辑**——所以必须循环到收敛，而不是「跑一次然后写下来」。

## 整改记录（外部复审 72bd0a4）

> **本节为该轮历史（历史@72bd0a4）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

审阅记录：`docs/reviews/2026-07-28-pr-7-scaffold-self-adoption-rereview-72bd0a4.md`，Request changes，3 项 Important + 1 项 Minor，六维 18/24。

| Finding | 裁决与整改 |
| --- | --- |
| **F1** 认证自检与最终裁决共享同一个仲裁器 | 接受审阅方案 1，并按结构理解落地。`certification_state()` 现在**只返回 layer 事实，不返回裁决布尔值**；每个出口从**它自己渲染的那些行**用 `all(ok)` 现场派生裁决——表头就是那张表算出来的，**「CERTIFIED 压在 FAIL 之上」在构造上不可能**。审阅方的原始变异（包住函数、保留 layer 表、只强制布尔值为 True）**已无对象可污染**。另加一道**与派生逻辑无共享**的产物级写入守卫：写正式文件前扫描**渲染文本本身**，发现 FAIL 行与 CERTIFIED 标记并存、两标记并存、无标记、未认证却非横幅开头 → 拒绝并 exit 1。**诚实残余**：若变异直接把每一行的 `ok` 都伪造为 True，渲染内部自洽，守卫无法察觉——那等价于 oracle 说谎，已写入披露。 历史@72bd0a4 |
| **F2** 「忘记注册也 fail closed」不成立 | 接受。未注册的产出者根本不会被执行，也就没有失败标签供兜底层捕获。整改用**最省的办法闭合第三个方向**：统一产出者命名约定（`selftest_*` / `_selftest_*`），**导入期扫描模块命名空间**，凡符合约定的可调用对象都必须出现在 `CHECK_CLASSES` 中，否则 `AssertionError`。同时把文档声明**收窄为四条实际保证**。实测：副本中定义未注册的 `selftest_forgotten` → 导入即失败，exit 1，不写任何文件。 |
| **F3** 最终态证据与模式契约未收敛 | 接受，做全域一致性通过。(a) **删除**全部「默认模式是唯一判定模式」「不要用 `--emit-markdown` 退出码判断」句子，改为一张**模式矩阵**作为唯一权威（default 与 emit 承载同一判定；baseline 是报告模式；help/参数错误是协议状态）；(b) 全部现刻结果位置统一到实测值（该轮认证自检 23 项，历史值，绑定当轮 HEAD `002b5d2`），历史段落保留旧值并加轮次标注；(c) 抽样句在**仓库记录全域**统一为「抽样层对每个命中输入做独立 oracle 分支 + 载体核对；不能证明的是未抽中输入」；(d) 内容改完后重跑收集器至不动点，并为上述三类句子各补审计行（19b / 20b / 21b）。 历史@72bd0a4 |
| Minor：提审提交数不准 | 接受。以 Git 图为准；本轮起提审前先跑 `git rev-list --count` 核对。 |

**本轮的方法学观察**：F1 是「**一个事实的两个副本**」。layer 表是事实，`certified` 布尔是它的副本——只要副本能独立存在，就能独立漂移，而自检恰恰用同一个函数去验证它自己。修法不是再加一层校验，而是**取消副本**：让裁决在每个使用点从事实现场派生。**能漂移的前提是存在两份；只有一份时，一致性不需要被检查。**

F2 补上的是注册关系的**第三个方向**。前两轮分别修了「失败无层接收」与「层无检查支撑」，这次是「检查未进注册表」。三个方向合起来才是闭包——而我每次只修了被指出的那一个。

## 整改记录（内部验证者复核，46666845）

> **本节为该轮历史（历史@46666845）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

新的内部验证者复放 R9 的五项修复：**全部成立**（仲裁器已无布尔可污染、发现断言连 lambda 都能抓、收敛干净）。另提 2 项 Important + 1 项 Minor，本轮关闭。 历史@46666845

| Finding | 裁决与整改 |
| --- | --- |
| **F-R10-1** 模式矩阵有一格为假 | 接受。`--emit-markdown` 指向**不存在的目录**或**目录本身**时抛 `OSError` 栈回溯、**exit 1**，而认证正在 PASS——既推翻了矩阵的「非法路径 = 2」，又把留给「判定失败」的 exit 1 花在 IO 错误上。整改：`target_path_problem()` 在参数校验阶段预检（目标非目录、父目录存在），写入处另包 `OSError` → 一律 **exit 2 + 明确 stderr**；argv 自检新增三项（不存在的父目录、目录型目标、可写路径），空/空白路径保留为回归；订正 `parse_args` 中那句被证伪的「exit 1 是留给……」注释。矩阵仍是唯一权威，且**每一格现已逐条实测**并记录。 历史@46666845 |
| **F-R10-2** 残余披露的前提为假 + 廉价补强 | 接受。上一版把残余写成「伪造 layer 事实后渲染内部自洽」——验证者证明**前提错了**：伪造 layer 行确实不留 `**FAIL**` 单元，但**证言计数**来自真实检查结果，`8/23 passed` 配 CERTIFIED 并不自洽。(a) 守卫新增**证言完整性检查**：CERTIFIED 在场时任一 `X/Y passed` 且 `X != Y` 即拒绝——验证者的原始探针实测被拒；(b) 残余披露改写为**按层精确**的表：定向层、四类自检层、未注册标签层的伪造**可被捕获**；**穷举层与抽样层的 `ok` 伪造未被覆盖**（这两层在表内只有汇总数字，不进证言计数，守卫也不解析层详情数字）。 历史@46666845 |
| **F-R10-3** 脚本 SHA 缺时点标注 | 接受。历史值加轮次历史标注（与相邻 R5 行同一约定），实测值统一读 机器生成的 `boundary-cases.md`。 历史@46666845 |
| 可选硬化（已采纳并披露） | 渲染器与守卫此前各自使用字面量 `**FAIL**`。现共用 `FAIL_CELL` / `PASS_CELL` 常量，并加一项自检实测「未认证渲染确实含 FAIL 令牌」——把耦合从假设变成测量。 |

**本轮的方法学观察**：F-R10-2 最值得记的不是漏洞，而是**我写的免责声明本身是错的**。 历史@46666845
我声称「伪造后渲染自洽所以抓不到」，验证者一看就发现渲染并不自洽（证言计数还在说真话）——
**我为了诚实而写下的残余风险，高估了自己的漏洞**，从而放过了一个本可以廉价关闭的检查。
免责声明也是断言，也需要被证伪；写「这个我抓不到」时，必须先真的去抓一次。

## 整改记录（内部验证者 delta 复核，aa23136d）

> **本节为该轮历史（历史@aa23136d）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

验证者复核确认 R9b 四项闭合**均在执行中成立**；另提 1 项 Important + 1 项 Minor。

| Finding | 裁决与整改 |
| --- | --- |
| **F-R11-1** 路径修复把文件系统探测放进了认证层 | 接受。验证者执行 `mkdir "$TMPDIR/out.md"` 就能在**未修改脚本**的情况下把认证翻成 FAIL——三条「无时钟/环境/文件输入」的现行陈述与「逐位可重现」契约因此为假；另有两处潜在依赖（硬编码的 `/no-such-dir-4f2a` 语义会随该路径是否存在而反转、一处读 `os.curdir`）。整改**只动自检**（生产行为本就正确）：三项路径检查改为在自己 `mkdtemp()` 出来的**私有目录**内构造全部三种形状并在 `finally` 中清理，不读共享 `$TMPDIR` 名字、不读 cwd、不用绝对根路径。确定性陈述重新限定为「无时钟输入、不读共享环境状态；自检对私有临时目录做真实文件系统操作，结果不受环境影响」。回归：验证者的 `mkdir` 探针实测**不再影响任何检查**。 历史@aa23136d |
| **F-R11-2** 再往前一行的廉价闭合 | 接受。守卫新增**层详情数字扫描**：CERTIFIED 在场时任何非零 `N mismatches` / `N failures` / `N undefined` / `N unrecognised` 即拒绝。验证者的两个外科式伪造（穷举 7、抽样 11 挂在 CERTIFIED 下）实测均被拒，并已作为两项自检回归固化。残余披露相应再次改写：按层表的最后一行由「未捕获」改为**捕获**；**现下真正剩下的只有一种**——把印出来的数字一并改掉的**整体伪造**，即渲染文本从头到尾自洽地说谎，与「oracle 本身说谎」同类，只能靠独立复现发现。 历史@aa23136d |

**顺带**：新增的路径检查辅助函数最初命名为 `_selftest_argv_paths`，**被导入期发现断言当场拦下**——它符合产出者命名约定却不是产出者。已改名为 `argv_path_checks`。这是 R9 那道断言第一次抓到我自己写的新代码。

**本轮的方法学观察**：F-R11-1 的形状是**测试把环境带进了判定**。生产代码读文件系统是对的——它就是要写文件；错的是**自检**读了共享路径，于是「认证是否通过」变成了「机器上恰好有没有这个文件」。判定层的输入必须全部由被判定物决定；任何来自环境的输入都会让同一份代码在两台机器上得到不同结论。**自检可以碰文件系统，但只能碰它自己造的那一份。** 历史@aa23136d

## 整改记录（全协议双盲自验 A/B，b9a12a09）

> **本节为该轮历史（历史@b9a12a09）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

会话 A（Standards）与会话 B（Spec）在**彼此不可见**的上下文中执行完整协议，双双 request-changes，且给出**不同的** Important。

| Finding | 裁决与整改 |
| --- | --- |
| **A-I-A**（Important）守卫只在 emit 出口运行 | 接受。默认模式的认证块没有独立守卫：A 的 layer-ok 伪造（不碰任何印出来的数字）在 emit 被拒 10 项，在默认模式却 **rc=0 全绿**；一行 `verdict()` 变异更能在默认模式打印 `certified: YES` 压在**六行 FAIL** 之上、仍 rc=0。整改：抽出**唯一**的 `render_certification_block()`，**两个出口渲染同一份输出**，并对该输出运行**同一个** `artifact_guard_violations()`——守卫从此**与出口无关**。回归实测：A 的两个探针在**默认模式**下现在均 `rc=1` 且打印 `ARTIFACT GUARD REFUSED`。 历史@b9a12a09 |
| **B-S-B1**（Important）cwd 通道仍在 | 接受。静态检查里 `parse_args(["--emit-markdown", "out.md"])` 传的是**相对路径**，`target_path_problem()` 会拿它对**进程 cwd** 求解——因此在**任意目录**里 `mkdir out.md` 都能翻转认证（B 实测）。整改：该检查移入 hermetic 的 `argv_path_checks()`，使用私有 `mkdtemp()` 目录内的**绝对路径**；并复查静态检查表，确认其余用到路径的检查都在触达文件系统之前就返回。回归：任意 cwd 下 `mkdir out.md` 实测**不再影响认证**（`/tmp` 与仓库根均 rc=0）。 历史@b9a12a09 |
| **B-S-B2**（Minor）三重伪造仍留可见矛盾 | 接受。守卫新增两项：CERTIFIED 在场却仍带「不认证」横幅、或仍写 `does NOT certify` → 拒绝。 |
| **A-M1 / A-M2 / B-S-B3**（Minor） | 审计表第 23 行的「24 项」订正为 27（**审计表自身也漂移了**）；新增三行覆盖此前未成行的能力声明（定制记录的覆盖范围 → 已限定；自检不读共享环境状态 → 构造保证 + 回归指针；输入/载体从不截断 → 构造保证）；`summary` 的「20 万次抽取」统一为 `200,000 次抽取`，陈旧的 `5/8/24/12` 加轮次历史标注并指向 机器生成的 `boundary-cases.md`。 |

**本轮的方法学观察**：A-I-A 与之前 R1 是**同一个形状的第三次现身**——守卫存在，但**只装在一个出口上**。
我每次都把新机制接到「当时正在修的那条路径」，而不是接到**所有输出该结论的路径**。
**一个安全检查的覆盖面等于它被调用的地方的并集，不等于它的实现质量。**

B-S-B1 则是「环境入侵判定层」的**残余实例**：上一轮我把三项路径检查做成 hermetic，
却漏了同一张表里的第四项——它没有出现在我脑子里的「路径检查」清单上，因为它的名字叫
「emit with path accepted」。**按名字分类去做清扫，会漏掉名字不像的那一个；要按行为扫。**

## 整改记录（外部复审 30f95e63）

> **本节为该轮历史（历史@30f95e63）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

审阅记录：`docs/reviews/2026-07-28-pr-7-scaffold-self-adoption-rereview-30f95e63.md`，Request changes，3 Important + 3 Minor + 1 nit。其中 **I3 与 Minor-3 是协调方侧的失误**（会话 C 结果只在对话中披露、从未入库；提审note 提交数记错），本轮整改把记录补全。 历史@30f95e63

| Finding | 裁决与整改 |
| --- | --- |
| **I1** 守卫不是载荷透明的 | 接受。守卫此前扫描整份文本，于是**载荷本身**若含 `**FAIL**`、`7 failures` 或认证标记，就会误伤。整改：`render_certification_block()` 把认证视图夹在**哨兵行**之间（`<!--CARRIER-SWEEP-CERTIFICATION-VIEW-BEGIN/END-->`，各须恰好出现一次——载荷行要么以 `\| ` 开头要么是散文，**不可能等于**哨兵行）；`artifact_guard_violations()` **只解析该视图**。审阅方四项回归全部实测通过：三种载荷（`7 failures`、`**FAIL**`、认证标记字面量）已**固化为定向用例**，全部 PASS，默认与 emit 均 exit 0，生成表可写且**逐字节稳定**；真实的非零层详情仍被拒；A-1 / A-2 / B-2 形状的伪造在视图内仍被抓住。定向用例因此由 69 增至 **72**，摘要相应变为 `ada0846f…`。 历史@30f95e63 |
| **I2** 抽样句第三次复发 | 接受。`summary` 中的方法学段落改为**按时点限定**：「**早期只检查分支归属、尚无独立 oracle 的旧扫描器**只能证明……那是**当时**的局限；**现行**抽样层对每个命中输入核对分支与载体，不能证明的是**未抽中输入**」。全仓库记录再扫一遍，剩余同族措辞均已带历史限定。 |
| **I3** 会话 C 运行未入库 | 接受。新建 `evidence/protocol-runs.md`：(a) 会话 C 的完整运行作为**历史条目**入库，绑定 `30f95e63`，逐步记录观察值与 READY 结论；(b) 本轮由我在新树态上**亲自执行**机械步骤（第 1/2/5/6/7/8 步）并记为**当前条目**，按「本记录随其所在提交生效」的约定标注；(c) 据此勾掉 tasks 的最后一格并引用该文件。**未入库的证据等于不存在**——这与「扫描表留在 git 忽略目录」是同一类错误。 历史@30f95e63 |
| **Minors / nit** | 陈旧的当前结果 digest `00788ca3` 加**当轮历史值**标注；依赖清单补 `re`、`shutil`；审计表行号**重排为连续序列**（并新增载荷透明一行）；本轮起报告逐字给出 `git rev-list --count <base>..HEAD`。 历史@30f95e63 |

**本轮的方法学观察**：I1 是**检查器与被检查物共用词汇表**。守卫用 `**FAIL**`、`N failures`
这些**字符串**判断状态，而这些字符串同时是**合法载荷**——于是「被测数据」能冒充「判定结论」。
修法是给结论划一块**载荷不可能进入的区域**（哨兵行），让守卫只读那里。
**当检查器靠内容特征识别结论时，必须先保证内容与结论在结构上可分离**，否则数据迟早会长得像结论。

I3 则重复了 G1 的教训，只是换了位置：**证据留在对话里和留在被忽略的目录里，是同一种不存在。**

## 整改记录（双盲自验 A/B 收敛，bfbd546）

> **本节为该轮历史（历史@bfbd546）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

会话 A 与会话 B 在彼此不可见的上下文中**独立收敛到同两个 Important**——按协调方裁定，收敛本身即高置信度信号。

| Finding | 裁决与整改 |
| --- | --- |
| **1（A-2 = S-1）载荷透明只做到守卫，没做到自检** | 接受。上一轮我把守卫改成只读哨兵界定的认证视图，却**漏了同一份文件里的 `selftest_certification()`**——它仍对**整份渲染文本**断言（`"does NOT certify" not in text_ok`、`"NOT CERTIFIED" not in text_ok`、`"EVERY input" not in text_bad`）。于是载荷 `b"NOT CERTIFIED\n"` 或 `b"log line: this run does NOT certify anything\n"` 就能翻转认证（两个会话都实测到了）。虽然是 fail-closed，但**正是上一轮宣称已关闭的那一类**。整改：自检中每一处文本断言都改为对 `certification_view(text)` 取出的视图做；两种载荷**固化为定向用例**，实测 PASS 且认证保持绿；订正 `carrier_sweep.py` 中那句过宽的「must not trip anything」注释与审计第 31 行，使「构造保证」名副其实。定向用例 72 → **74**，摘要变为 `866f00cd…`。 历史@bfbd546 |
| **2（A-1 + S-2）陈旧数值，其中两处伪装成实测转录** | 接受，且这一条比数值本身更严重：`summary` 中标着「**当前脚本上重跑**」的 `_digest` 打桩块与 `$`-引导的红基线转录，**看起来是实测输出，实际是旧值**——我自己的 69 → 72 改动已经让它们失效。整改：两处**重新执行并逐字粘贴**（打桩后 `directed_failures=47`、`sampled_mismatches=151811`、digest `0cfb3bf0…`；红基线 `directed cases: 75, failures: 30`）；三处当前语态的 69 改为 74；`run-manifest` 早期方法学段落的裸 69 加**当轮历史值**标注；`summary` R8 行的「认证自检 9 项」同样加历史标注。 历史@bfbd546 |
| **Minors** | 审计第 12 行的导入清单补 `re`、`shutil`；新增审计第 32 行覆盖本轮出现的新全称句「载荷行不可能等于哨兵行」（并写明其残余：依赖渲染器不新增整行任意字节的分支）；**收集器词表扩充** `不可能` / `must not` / `never` 并重新收敛——同时把规则写进 manifest：**收集词表必须随声明词汇一起增长**，否则枚举基自身会漂移；`protocol-runs.md` 的当前条目约定写成三条明文（提交前事实入库、不记录自身 SHA/CI、验证跳转点是 PR 正文）。 历史@bfbd546 |

**本轮的方法学观察**：Finding 1 是**同一修复没有走完它自己的作用域**。我把「只读认证视图」这条纪律施加给了守卫，却没有施加给**同一文件里另一个也读整份文本的函数**——修复的边界停在了「我改的那个函数」，而不是「所有读这份文本的地方」。这与 R9d 的 A-I-A（守卫只装在一个出口上）是同一个错误的第四次出现：**每次我都把新纪律接到当时手里那一处，而没有先枚举「还有谁做同样的事」。** 历史@bfbd546

Finding 2 则揭示了一种更隐蔽的失真：**把陈旧数值写成实测转录的样子**。`$ 命令` + 输出的排版本身就是一种断言——它声称「这是跑出来的」。当它其实是上一轮的旧值时，读者无从分辨。**凡以转录形式呈现的内容，必须在该次交付中真的重跑过**；否则应当去掉 `$` 排版，或标注时点。 历史@bfbd546

## 整改记录（会话 C 机械验证，c008666）

> **本节为该轮历史（历史@c008666）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

会话 C 复核：**机制全绿、A/B 反例全部关闭**；但提出 3 项**报告完整性**类 Important + 3 项 Minor，全部为文本。

| Finding | 裁决与整改 |
| --- | --- |
| **1** `protocol-runs.md` 现刻条目沿用了上一提交的数值 | 接受，而且这是**本轮最难看的一条**：这份文件正是为「未入库的证据等于不存在」而建，它的现刻条目却**逐字沿用 `bfbd546` 的数值**（脚本 SHA `3d3c46bf`、摘要 `ada0846f`、定向 72）——**为防止陈旧转录而建的文件，自己成了陈旧转录**。整改：在**最终内容态**重跑机械步骤，粘贴**本提交**的实测值（脚本 SHA、摘要、定向数、前驱 SHA、增量统计）。 历史@c008666 |
| **2** manifest 里印出的 `rg` 命令未含扩充词 | 接受。上一轮我把 `不可能`/`must not`/`never` 加进了**执行时使用**的词表，却没改**文档里印出来的那条命令**——于是「按此命令可得此计数」不成立。已订正，并重新迭代收敛（改这几行本身就会改变计数）。 |
| **3** 审计第 12 行的导入清单未落地 | 接受。`summary` 与 `tasks` 的 `[x]` 声称补了 `re`、`shutil`，**实际没落到行内**。已真正落地。 |
| **Minor** 第 31 行未真正订正 | 已订正：「三种载荷」→**五种**，并写明该保证由**守卫 + 自检视图取值**两处共同成立（缺一不可，R10b 之前自检读整份文本正是入口）。 |
| **Minor** 第 27 行残余过窄 | 已加宽：除「连数字一并伪造的整体渲染」外，另一类是**注册类零执行**的渲染（`0/0 passed`、无非零数字、无 FAIL 单元）——**守卫看不出矛盾**，由 `certification_state()` 的兜底层拦下。两道防线分工不同：守卫抓「渲染自相矛盾」，认证状态抓「事实本身不成立」。 |
| **Minor** 历史标注不统一 | 已统一：`run-manifest` 的 M10 段、`summary` 的红基线 Minor 行、`carrier_sweep.py` 的 oracle 注释三处裸值均加上轮次历史标注并指向 机器生成的 `boundary-cases.md`。 |

**新增常设纪律（本轮教训，即刻生效）**：**报告本身也是产物，其声明必须对树做机械核对。**
今后每一条「已在 Y 处修好 X」的报告行，都必须附一条 `grep`/`diff` 证据显示该编辑**确实存在于 Y**，
并把这份**落地核对清单**随报告提交。本轮三项 Important 中有两项（第 2、3 条）就是 历史@c008666
「声称已改、实际未改」——`[x]` 是一种断言，而我此前从未机械核对过它。

**本轮的方法学观察**：第 1 条与第 3 条是同一个病灶的两种形态—— 历史@c008666
**把「打算做的」写成「已经做的」**。第 3 条是勾选框跑在编辑前面；
第 1 条更甚：为治这个病建的文件，自己犯了这个病。
**任何自称记录事实的产物，都必须在它所描述的那个状态上重新生成，而不是被继承下来。**
继承是默认行为，重跑必须是显式动作——所以它需要一条纪律，而不是一次注意。

## 整改记录（外部复审 351c70a）

> **本节为该轮历史（历史@351c70a）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

审阅记录：`docs/reviews/2026-07-28-pr-7-scaffold-self-adoption-rereview-351c70a.md`，Request changes，3 Important + 1 Minor。

| Finding | 裁决与整改 |
| --- | --- |
| **I1** 认证视图可提前结束 | 接受。`certification_view()` 此前只校验**边界**（哨兵各一次、有序），不校验**结构**。把唯一的 end 哨兵移到 CERTIFIED marker 之后、FAIL 表之前，边界仍然合法，守卫返回空问题——而整篇文本同时含 CERTIFIED 与 FAIL。整改：视图现在校验**结构完整性**——marker 恰好一个；layer 行与 `CERT_LAYERS` **完整、唯一、同序**；attestation 与 `CHECK_CLASSES` **完整、唯一、同序**；因此 end 哨兵必然位于这些事实之后。**审阅方反例实测被拒**，另加 premature end / late begin / 空视图 / 仅 marker / 缺 layer / 缺 attestation **六种形状**的回归，全部被拒；真实非零层在**两个出口**仍 fail closed。 |
| **I2** 自指统计第三次复发 | 接受，并按设计方裁定做**类修复：去镜像**。记录里写「4 文件 `+42/-8`」而 Git 是「5 文件 `+60/-27`」——根因不是粗心，而是**把可事后导出的统计量抄进记录**：记录与它所在的提交互相追逐，永远差一步。整改：**删除记录中一切 Git/命令可导出的统计量**（差异统计、文件数、`rev-list` 计数），代之以**待执行的命令**；现刻条目只保留两类事实——**机器生成产物本身**，以及**产物不变量**（脚本 SHA、结果摘要；它们描述的是产物内容，不随「哪个提交包含它们」而变，故无自指）。其余降为**带时点的历史**。PR 正文的 CI 指针改为 **exact-head** run，并把「push 后刷新 run id」写进指针刷新步骤。 |
| **I3** 终检与核对清单未进入仓库完成态 | 接受。我在**最终内容态**自跑 C 式机械终检并**落库**到 `protocol-runs.md`（去镜像格式：命令 + 关键不变量）；**逐项落地核对清单**也落进同一文件（此前它只在报告里，属 gitignored 之外的另一种「不存在」）；随后才勾选任务。最终树不留非预期未完成项。 |
| **Minor** 起始哨兵字面量未入库 | 接受。该字面量此前只在会话 A 的 `/tmp` 探针里出现过，提审却称已固化。现已**作为定向用例提交**，实测 PASS；定向用例 74 → **75**。 历史@351c70a |

**本轮的方法学观察**：I2 的「去镜像」值得单独记。前两次我都是**把数字改对**，
这一次改的是**要不要在这里写数字**。一个记录若包含「描述它自己所在提交」的统计量，
就存在一个结构性的追逐：改记录 → 统计量变 → 记录又错。
**能被命令导出的事实，不该被抄进记录；记录应当给出命令。**
这与更早的「收集器不动点循环」是同一条原理的两种手段：
那次是用循环强行收敛，这次是直接取消需要收敛的东西——**后者更强，因为它消除了问题本身。**

I1 则是「校验了容器的边界，没校验容器的内容」。哨兵回答的是「视图从哪到哪」，
而守卫真正需要的前提是「这段文本确实是一份完整的认证」。
**边界合法不等于内容完整**——一个片段可以完全合法地被界定出来，却什么也没证明。

## 整改记录（双盲自验 A/B 收敛，baf6797）

> **本节为该轮历史（历史@baf6797）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

会话 A 与 B 独立收敛于「脚本可导出数值的手抄件陈旧」（第四次复发）与「核对清单自相矛盾」；
另各有一处独有发现。

| Finding | 裁决与整改 |
| --- | --- |
| **1（A-C1 = B-S-I2）终极类修复：把去镜像推到脚本可导出的一类** | 接受。第 11 轮我只删掉了 **Git 可导出**的统计量，规则本身写的却是「Git **或命令**可导出」——于是脚本可导出的一类（定向数、四类自检项数、穷举/抽样总数、唯一抽样数、分支直方图、各探针 digest）继续手抄，本次提交把认证自检从 27 改到 33，抄本没跟上，**第四次复发**。整改：**这些数字全部从现刻语态记录中删除**，代之以「运行命令 / 读机器生成的 `boundary-cases.md`」；只保留两个**产物不变量**——脚本 SHA 与结果摘要（它们描述产物内容，不描述「哪个提交/哪次运行包含它」，故无自指）。历史小节保留其带时点标注的数字。彼时我据此写下「此后现刻数字只存在于机器生成产物中，陈旧数值这一类在结构上不再可能」——**该句已于 R13 缩域、并于 R14 再次改写**：它由有界前提推出无界结论，先后被 C 会话在 `run-manifest.md` 与 `summary.md` 的残值证伪两次。现行可声称的仅是：除机器生成产物外，「现刻标记词 + 数字」同行出现即违规，由 `check_current_numbers.py` 全文核查。 历史@baf6797 |
| **2（B-S-I1）umask 韧性** | 接受。`argv_path_checks()` 的文件系统构造没有兜底：`umask 177` 下 `mkdir()` 造出不可用目录，抛出**裸 traceback、exit 1**——环境问题穿上了留给「判定失败」的退出码。这是 F-R10-1 换了位置的第三次现身。整改：构造包在 `try/except OSError` 中，失败降级为 argv 层内一条**具名失败检查**——认证块照常渲染、退出 1、原因可读。审计第 28 行由「构造保证」改为**已限定**：已闭合的共享状态逐项枚举（`$TMPDIR` 共享名、cwd、硬编码路径），`umask` 作为**已承认的未闭合项**写明其降级行为。实测：`umask 177` → exit 1、**无 traceback**、NOT CERTIFIED 且点名 argv 层；`umask 022` → 全绿。 历史@baf6797 |
| **3（A-I1 = B-M2）核对清单自相矛盾** | 接受。上一轮 I2 那一行期望「0」，而实际必然是「≥1」（病灶引述句本身含该模式），我却把它记成通过。现改为**锚定排除 + 真实期望**：命令用 `grep -v '^[0-9]*:\|'` 排除清单自身那一行，期望恰 **2** 行并**逐行点名**（第 11 轮病灶描述、现刻条目引述）。**核对命令若把自己算进去，就是又一个自指**——这句话已写进该行。全部 `<evidence>` 占位符替换为可直接运行的真实路径。 |
| **4（B-M1）late-begin 回归名不副实** | 接受。该回归喂的是「**删除**起始哨兵」的文档，而不是「起始哨兵**后移**」。现已改为真正把 `CERT_VIEW_BEGIN` 移到 attestation 之前，并**另加**一条「removed begin」回归——两种形状各有其名，结构攻击回归由六种增至**七种**。 |
| **5 Minors** | USAGE 概要补全（`-h`/`--help` 与 `PATH` 操作数的位置）；残余披露补一行：视图中混入**未注册的良性行**结构校验看不见（按注册表比对，不枚举多余行），但**不可达**——非零数字与 `**FAIL**` 单元仍分别被数字扫描与 FAIL 扫描拦截，替换真实失败行会立刻造成「缺 layer」被拒。 |

**本轮的方法学观察**：第 1 条是**规则写对了、执行只做了一半**。第 11 轮我写下的规则是 历史@baf6797
「Git **或命令**可导出的事实不该抄进记录」，但我只执行了 Git 那一半——
**因为那是审阅方当时指出的那一半**。同一份规则里未被指名的另一半，
就这样又活了一轮，直到它自己复发。
**当我写下一条带「或」的规则时，必须当场枚举它的每一个分支并逐个执行**；
否则规则的覆盖面会退化成「被指出的那部分」。

第 2 条则是同一个「环境入侵判定层」第三次换位置出现：先是 cwd、再是共享 `$TMPDIR` 名、
这次是 `umask`。前两次我关闭的是**具体路径来源**，而这次说明：
**只要自检要碰真实文件系统，环境就永远有一个新入口**。
因此这一行的正确形态不是继续追加「构造保证」，而是**改判为「已限定」并枚举已闭合项**——
承认这条边界是开放的，比声称它已封闭更接近事实。

## 整改记录（会话 C 终验，a48e9b65）

> **本节为该轮历史（历史@a48e9b65）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

会话 C：**机制层全部通过**（umask 三探针、11 种伪造形状全拒、收集器不动点精确复现、七门禁全绿、零回灌）；
**报告层三项 finding 成立**。本轮为微修。

| Finding | 裁决与整改 |
| --- | --- |
| **C1（Critical）残值** | `run-manifest.md:49` 残留「当前定向用例为 74」，实际值更大——**去镜像化那次提交自身漏网的当前态脚本导出数字，且已过期**，第五次复发。已删除；同处的 69 作为 R7 轮历史值保留并**绑定当轮 HEAD** `50a6060`。 历史@a48e9b65 |
| **I1（Important）残值不止一处 + 无界声称** | 接受。会话 C 列出的九处（含逗号格式 `65,793` / `200,000` / `137,527` 导致初扫漏检——**这正是 ∀ 收集器不完备模式在其后继规则上原样复发**）已逐一处置：命令可推导的删值留命令、历史轮记录移入历史语境并绑定 HEAD、`boundary-cases.md` 可读的改为指针。另外两句无界声称——`run-manifest.md:430-431`「结构上不再可能」与 `protocol-runs.md:66`「现刻数字只存在于机器生成产物中」——**均被 C1 证伪**，已删除并改写为**有界**形态。 |
| **I2（Important）规则没有能失败的检查** | 接受，这是本轮最实质的一条。∀ 收集器匹配**关键词**而不匹配**数字**（`run-manifest.md:49` 根本不在其命中集内）；唯一相关的 checklist 行锚定在单一小节的两个字面短语上，残值一旦落在窗口外就恒返回 0。**「检查必须先证明自己能失败」这条纪律，在去镜像规则上缺位。** 整改：新增 `evidence/check_current_numbers.py`——扫描指定区间内的全部数字 token（含逗号格式），减去**显式白名单**，剩余非空即 exit 1；并内置 `--self-test` 做**变异验证**（内存中注入一个杂散数字，断言必被抓住，不修改任何文件）。三条 checklist 行记录其正常态、变异验证与参数域。 历史@a48e9b65 |
| **M1** | OSError 回退此前把注入的真实生产缺陷也归因为「fixture could not be built」。语义无误（恒 fail-closed，会话 C 已确认），但归因不实。改为中性措辞：`argv path fixture construction raised OSError (…) -- environment restriction or script defect; either way FAIL`。 |
| **M2** | 审计第 32 行写六种形状，脚本实测七种。已改为**七种并逐一具名**（premature end、late begin、removed begin、空视图、仅 marker、缺 layer、缺 attestation）。 历史@a48e9b65 |

**如实披露**：这是对上一提交 `a48e9b65` 那句「终极类修复」声称的**缩域修正**——
该声称的域大于其真值域（它宣称一类缺陷「在结构上不再可能」，而残值当场存在）。
本轮把它换成**白名单 + 可失败检查**的构造保证：能声称的只是
「白名单之外的数字会被这条命令抓住」。

**本轮的方法学观察**：I2 指出的缺位最值得记。我在多轮里反复要求**被测对象**先证明能失败
（红基线、变异探针、比较器自检），却**没有对我自己新立的规则提出同样要求**——
去镜像规则从提出到复发经历了两轮，期间没有任何一条命令能够因它被违反而变红。
**给别人立规矩时要求可证伪，给自己立规矩时却只写纪律**，这是一种不对称。
一条没有失败模式的纪律，与一句愿望没有区别。

## 整改记录（R13 增量终验，fd8032ca）

> **本节为该轮历史（历史@fd8032ca）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

四项 finding 成立，其中 **C1 是同一缺陷第六次复发，且为字符串原文复现**。

| Finding | 裁决与整改 |
| --- | --- |
| **C1** 同一字符串在别处原样留存 | 接受。R13 我按会话 C 给的行号修了 `run-manifest.md`，**没有对同一字符串做全目录检索**——`summary.md` 里逐字相同的一处原样留存。整改分两层：文本层按**内容寻址**清理（先 `grep -rn` 拿全部命中再逐一处置）；纪律层把「finding 必须内容寻址、行号仅作起点」写进 `protocol-runs.md` 的落地清单说明。 |
| **I1** 过期值 + 四处死指针 | 接受。过期的自检项数已删除；`summary.md` 里根本不存在的「当前结果」一节导致的四处前向指针，全部改指**机器生成的 `boundary-cases.md`**——指针指向不存在的小节，比数字过期更糟：它让读者以为别处有权威值。 |
| **I2** 检查器被两处击穿 | 接受，且这是本轮的类修复。旧检查器有两个可绕过点：**反引号置盲**（数字加一对反引号即隐身，而反引号恰是本记录主流写法）与**行号窗口**（域外文本根本不扫）。**不再修补窗口，改换防守形态**：规则重述为**句式禁令**——除机器生成产物外，记录任何 `.md` 中「现刻标记词 + 数字」同行出现即违规；当轮的检查域是**整个 Change Record 目录全部 `.md` 全文**，无窗口、无置盲、反引号照扫（历史@c399394；**该域此后已扩展**——R15 起含 `.py` 注释与 docstring，当前覆盖与开放边界以 `evidence/run-manifest.md` 的载体自由度表为准）。豁免仅两类且机器可判：历史绑定行（含「历史」且含七位以上十六进制 HEAD）与两条产物不变量。 |
| **I3** 扫描窗口结构性复制了旧弱点 | 与 I2 同一修复：域改为整目录全文后，窗口这个概念本身消失，`SCAN_TARGETS` 嵌套问题（M3）随之自然消失。 |
| **M1** 已撤回的声称仍以真陈述口吻出现 | 接受。该句已就地改写为撤回口吻，并注明它被证伪**两次**（R13 与本轮各一次），不再依赖数十行之后的补丁去纠正读者印象。 |
| **M2** 「当前态小节」域从未枚举 | 与 I2 同一修复：域不再需要枚举——它就是整目录全文。原白名单+窗口方案在 ∀ 审计表中标记为**已撤回**，并写明两个被击穿点。 |

**变异验证升级（历史@c399394）**：当轮把 `--self-test` 从单文件单记法改为**逐文件注入**，记法轮换（裸数字 / 逗号格式 / 反引号包裹），并额外验证每种记法至少触发一次。**该形态已被后续轮次两次取代**——R15 改为真叉积（每文件 × 每变体），R17 再加单行 docstring 变体；现状以检查器 `--self-test` 的实际输出为准，覆盖范围见轴表。当轮的教训仍成立：**只在一个文件、一种记法上证明过能失败的检查，就只在那里被证明过。**

**如实披露**：R13 的检查器**在其提交时刻即未覆盖正在发生违规的文件**——`summary.md` 从不在它的扫描域内。本轮以**句式禁令 + 全文域**替换窗口白名单。

**本轮的方法学观察**：C1 的形状是**按坐标修复而非按内容修复**。审阅给的是行号，我就修了那一行——
但缺陷的身份是**字符串**，不是坐标。同一个错误在别处逐字存在，而我从未去找。
**收到 finding 时，第一个动作应当是把它的模式在全域检索一遍，而不是打开它给的那一行。**

I2 则说明：**检查器的每一处「为了少报噪声」的让步，都是一个可被利用的入口。**
反引号置盲当初是为了让输出可读——代价是任何数字都能靠一对反引号隐身。
噪声该用**更精确的禁令**（句式）去降，而不是用**盲区**去降。

## 整改记录（R14 终验，c399394a）

> **本节为该轮历史（历史@c399394a）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

防守形态本身被确认**真实更强**（十八项变异全捕、无窗口无置盲）；但**域闭合只沿被击穿的轴做了**，
留下两条开轴，缺陷立刻在第一条上**第七次复发**。

| Finding | 裁决与整改 |
| --- | --- |
| **C1（Critical）第七次复发，藏在被认证脚本的注释里** | 接受。`carrier_sweep.py` 的一处注释写着定向用例的两个计数，其中一个自三个提交前起已过期——而检查器只 `walk` `*.md`，**结构性看不见它**。整改：注释中的计数**全部删除**（改为不含数字的表述）；域扩展到 `.py` 的**注释与 docstring 行**（代码行不扫，数字是代码常态）。脚本 SHA 随之变化并已同步记录；**结果摘要不变**——注释不影响行为，这一点在提交说明中写明。 |
| **I1（Important）标记词表漏掉语料主导词汇** | 接受。词表缺了**现在 / 现行 / 实测 / 本轮 / 最新 / 共**——这些正是本记录最常用的现刻说法，其下现存多处未守护的脚本导出数字。整改：中文词表并入上述词，英文并入 `now / currently / at present / as of`（供 `.py` 注释域）；计数词 `共 / 合计 / 总计` 要求与量词共现，并排除 `共因 / 共享 / 共同` 等复合词。扩表后全部新命中逐一处置：删值留命令 / 迁移为历史绑定 / 指针化。 |
| **I2（Important）历史豁免与内容无关** | 接受，这条最危险。旧豁免是「同行出现『历史』+ 任意 7 位以上 hex」即**整行放行**——于是**顺手引用一个 commit id 就能走私任意现刻数字**，终验实测两条逃逸均未被捕获。整改：豁免改为**显式绑定记号** `历史@<hex>`，必须被刻意写下；全库既有历史行已迁移到该记号；两条逃逸样本**固化为自检变体**，现均被捕获。 历史@c399394a |
| **M1/M2 清单期望值又错** | 接受——**自计数缺陷在认证修复的那一行复发**。两行期望值已重写为显式枚举，并改用能自证的命令（检查器退出码 + 自检叉积），不再依赖人工数「几处命中」。 |
| **M3 「第 N 行」豁免** | 接受。该豁免恰是我们自己纪律禁止的寻址方式、也是最易漂移的数字。已移除，命中处改为内容锚或历史绑定。 |
| **M4 自检打印与实现不符** | 接受。旧实现是「每文件单变体轮换 + 仅首个文件全变体」，却打印「every file × every notation」。已改为**真叉积**（每文件 × 每变体全注入），打印句同步为实际规模。 |

**本轮的方法学观察**：这一条比前几轮都更结构化——**域闭合必须枚举全部载体轴，而不是只闭合被击穿的那一条。**
R14 我沿「行号窗口」和「引用记法」两条被指出的轴做了闭合，且做得确实更强；
但「载体」还有另外两条轴我从未列举：**文件扩展名**与**标记词汇**。
缺陷没有消失，它只是移动到了未被枚举的轴上——并且**恰好落在被 SHA 认证的脚本注释里**，
一个「已被证据链覆盖」的错觉最强的位置。

**判据**（**已于 R16 执行**，见 `evidence/run-manifest.md` 的「载体自由度表」）：再声称「某类缺陷的域已闭合」之前，先把**载体的每一个自由度**列出来
（文件类型、语法位置、词汇形态、时态标记、引用方式），逐条说明它是被覆盖还是被承认开放。
**没有被列举的自由度，就是下一次复发的位置。**（R16 已把这条从「标准」变成「交付物」：轴表随记录入库，逐轴标注覆盖/开放。）

## 整改记录（R15 终验，26ef797a）

> **本节为该轮历史（历史@26ef797a）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

机械声称全部核实（叉积、逃逸样本、SHA/摘要同步、七门禁），但**第八次复发成立**，
且这次发生在**核对清单自身**——上一轮新写的两行清单行在被取代后未删除，
继续断言「每文件单变体轮换」与「`.md` 全域」，而**同一提交刚把前者定性为缺陷**。

**本轮是方向切换，不是又一轮加固。**

| Finding | 裁决与整改 |
| --- | --- |
| **第八次复发：清单废止行未删** | 接受。两行已删除，并把**清单纪律**写进 `protocol-runs.md`：**清单的不变量是「每行当下可复跑且期望成立」，不是「只增不删的历史」；新行取代旧行时旧行必须同提交删除**。`tasks.md` 中「重写」一词按事实改为「增补，废止行于 R16 删除」。 |
| **I1 检查器输出串在扫 `.py` 时为假** | 接受。输出改为如实措辞（`.md` 全文 + `.py` 注释行）。注意该串位于**代码行的字符串字面量**中——正是轴表标注为**开放**的那一类，机器不覆盖，靠内容寻址修复。 |
| **I2 三条轴开放未披露** | 接受，这是本轮**核心交付**：新增**载体自由度表**，逐轴标注覆盖/开放——文件类型、语法位置、数字形态、时态标记、豁免条件、产物本身。实弹 `52 个定向用例`（无标记裸数字）已按内容寻址处置为指针。 历史@26ef797a |
| **M1 Group E fixture 注释自称 real gate outputs** | 接受。该注释描述的是**快照**，其中的测试数与耗时会静默漂移。已改为明示：`历史@444cc95` 快照、非当前实测、只用于在真实形状上练习载体规则。 |
| **M2 生成表字节再生未入清单** | 接受，已提为正式清单行（`--emit-markdown` 到临时文件后 `cmp`）。 |
| **M3 `历史@` 整行放行未披露** | 接受。**不改代码**（收紧到 token 粒度会把检查器推向解析器形态），改为在轴表中作为**已知宽豁免**披露：一个合法历史值会连带洗白同行其他数字。 |

**方向切换的理由（八轮证据）**：检测器每强一轮，缺陷迁一轴——
行号窗口 → 引用记法 → 文件扩展名 → 标记词汇 → **清单自身**；
且**每轮修复新增的声称行本身成为下一轮的复发点**。
这不是执行不力，而是结构性的：**每扩张一次检测能力，就多出一片需要被证明为真的声称面**。

因此终局形态定为 **声称缩面 + 轴枚举披露**，检测器**冻结**在现状：

> 在载体自由度表标记为**覆盖**的域内无违规——构造保证，`exit 0` 可复跑，
> 失败能力由文件 × 记法叉积自检证明。**开放轴见表，属已限定披露。**

**本轮的方法学观察**：前七轮我一直在回答「怎样让检测更强」，
而第八次复发给出的答案是：**再强的检测，其修复动作本身也在生产新的可证伪断言**。
当一个防御体系的每次加固都等量增加待证明面积时，收敛的方向不是继续加固，
而是**把不能被机械保证的部分明确让出去**——写进表里，标注为开放，交给纪律与外部审阅。
**能被机器保证的要缩到最小且可复跑；不能被保证的要显式承认，而不是用更复杂的机器去假装覆盖。**

## 整改记录（R16 终验，6d8a6fcc）

> **本节为该轮历史（历史@6d8a6fcc）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

清单逐行全过、轴表探针基本吻合、门禁与 CI 全绿；但**第九次复发成立，且与 R13 C1 完全同形**。

| Finding | 裁决与整改 |
| --- | --- |
| **第九次复发：删了行，没删断言** | 接受，且这一条最难看：我删除了两行废止清单行，**却没有按自己在同一文件里写下的内容寻址纪律**，对**这两行的断言字符串**做全库 `grep`——它们的原文继续活在叙事段里，而同一份记录在另一处已把其中一句定性为缺陷。**类修复 A**：`summary.md` 各轮叙事小节按历史处理——以现在时描述机制的句子，或加 `历史@<该轮 HEAD>` 绑定，或改写为「当轮改为……（此后已再变，现状见轴表 / 检查器）」。 |
| **类修复 B：纪律必须变成格子** | 内容寻址纪律写进文档两轮后仍被违反。因此新增**固定小节「内容寻址扫描记录」**：每轮必填，列出该轮每个 finding 涉及的字符串 / 模式 × 全库命中数 × 逐处处置终态，并附可复跑的核查命令。**空表即视为未执行。** |
| **I2 PR 正文复活无界句** | 接受。已改为缩面表述：机器保证仅及轴表覆盖域；`boundary-cases.md` 是现刻数字的唯一生成权威，记录其余数字为命令 / 白名单 / 历史绑定——与开放轴披露一致。 |
| **C2 单行 docstring 永不被扫** | 接受。三引号同行成对，旧解析只认奇数个，于是单行 docstring 整行被跳过——而轴表声称「`.py` 注释与 docstring = 覆盖」。**这不是检测器扩张，是已声称覆盖域内的实现缺陷**，构造保证必须补齐：已修复，并新增单行 docstring 自检变体。同时确认反向不误伤：**仅包含**三引号字面量的代码行仍属开放轴，不被扫。 |
| **M1 更替纪律被本提交违反** | 接受。上一提交写下「被取代行必须同提交删除」，自己却留下两行旧行。已删除。 |

**本轮的方法学观察**：第九次与第六次**完全同形**——都是「按坐标修复、不按内容修复」。
区别在于，第六次之后我把内容寻址写成了纪律，而它**仍然没有被执行**。

原因不在于纪律写得不清楚，而在于**纪律是叙述，清单是动作**：
交付前我会逐行跑清单，但不会逐条重读纪律段落。
因此本轮把它做成**必须填写的表格小节**——**没有格子的纪律不会被执行。**

推论：任何要求「每次都做」的事，都应当在交付清单里有一行需要填写的证据，
而不是在文档里有一段需要被记住的话。

## 整改记录（R17 终验，63b853c0）

> **本节为该轮历史（历史@63b853c0）**：节内现在时均指**当轮时点**，不描述现状；机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

字面串终态、清单逐行、C2 修复、外审入口命令全部通过；但**第十次复发成立**——内容寻址做在了**字面字符串**上，而不是**断言**上。

| Finding | 裁决与整改 |
| --- | --- |
| **第十次复发：绑字面串，没绑断言** | 接受。被废止的两条断言有四处**活体转述**未被处理（措辞与被删行不同字，字面 grep 自然搜不到），其中一处恰在我已绑定的那一行**正上方**。**类修复：断言级扫描**——除字面串外，对该 finding 的**机制关键词**逐词全库 grep，每个命中归类为 SSOT / 指针 / 节级历史域 / 违规，归类表随扫描记录提交。 |
| **I2 类修复只处理了两句而非类** | 接受。逐句贴标签消灭不了镜像种群。**结构性收束**：机制描述设**单一事实源（SSOT）**——`run-manifest.md` 的「检查器现状」节（轴表 + 机制要点 + 冻结条款）是唯一权威；其余位置一律改为**指针**或落入**节级历史作用域**。 |
| **I3 冻结条款与 R17 改动矛盾** | 接受。冻结条款此前只在别处提到豁免。现把豁免写进 SSOT 的冻结条款本身：**已声称覆盖域内的实现缺陷，修复不受冻结限制**——补齐实现使其符合既有声称，不是扩张声称；R17 的单行 docstring 修复即属此类。 |
| **节级历史作用域（本轮定型）** | 各轮叙事节的标题下统一加一行声明：「本节为该轮历史（历史@HEAD）：节内现在时均指当轮时点」。∀ 审计表加一次性表头声明。**此后叙事内现在时合法**，不再逐句贴标签——镜像要么归历史域、要么变指针、要么在 SSOT。 |
| **M1 扫描记录首次填写即有假格** | 接受。「三种记法 summary 一处」实为 0，已改真值，并在格内写明教训：**填表本身也要复跑，不能凭印象**。 |
| **M2 R17 叙事又造新错数** | 接受。该处计数去数字化（它本就是可导出量）。 |
| **M4 / M5 / M6** | PR 正文补扫描记录小节与自检叉积规模；扫描排除由**文件级**改为**节级**（文件级排除会掩掉同文件其他位置的真违规）；清单第 4 行期望改**内容锚**，不再用行号坐标。 |

**本轮的方法学观察**：前十轮我一直在做**个体治疗**——发现一处、修一处、再写一条纪律要求下次别犯。第十次复发说明这条路走不通：**同一断言的转述是一个种群，不是一批个体**；只要允许它在多处独立存在，改一处不改其余，种群就继续生育。

**结构性收束的形状**：**单一事实源 + 作用域**。
机制现状只允许在一处描述（SSOT），其余位置要么是指向它的指针，要么落在明确声明了时点的历史作用域内——在作用域内，现在时是**合法**的，因为读者已被告知它指的是哪一刻。

这样一来，**声称面不再随轮次增长**：新一轮只更新 SSOT 一处，历史节自动保持正确（它们本就只声称当轮事实）。
**能被逐句追杀的缺陷，说明它还不够多；多到追不过来时，就该改变它们能存在的结构。**

## 整改记录（R18 终验，6507a986）

> **本节为该轮历史（历史@6507a986）**：节内现在时均指**当轮时点**，不描述现状；
> 机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

SSOT + 节级作用域被终验证实有效——但**缺陷迁移到了重构未覆盖的死角**：
上一轮的重构只走了 `.md` 的中文措辞，域的另外两个半边（`.py` 文件、英文措辞）没被走到。

| Finding | 处置 |
| --- | --- |
| **C1（Critical）纯 ASCII 不变式被自己的修复打破** | 接受，且这是本轮的类缺陷。R16 给 `carrier_sweep.py` 的 Group E 注释贴 `历史@` 标签时，把非 ASCII 字节写进了**被哈希存证的纯 ASCII 脚本**，使五处「源码纯 ASCII」的活体声称同时为假。**历史绑定惯例与 ASCII 不变式正面冲突**：绑定已移入 `run-manifest.md`（`.md` 内用 `历史@` 无冲突），脚本注释恢复纯 ASCII 且不含数字，并新增一条**能变红的清单行**守护该不变式——它被声称十一轮却从未有格子守护。 |
| **I1** 检查器 docstring 自成第二事实源 | 接受。模块 docstring 重写为**指针式**，只说「机制权威在 SSOT 节」，不再自报域、词表、豁免族数与自检形态。 |
| **I2** `run-manifest.md` 白名单节既过包含又欠包含 | 接受，整节撤回，替换为指向 SSOT 的指针并说明旧框架为何被撤。 |
| **I3** 载体自由度表游离于 SSOT 之外 | 接受，降级为 SSOT 节内的 `###` 子节。 |
| **I4/M8** 节级排除写法失效且域对 `.py` 失明 | 接受。旧 `scan_only` 用 `awk` 拼 `(a\|b)` 喂基本正则 `grep -v`，**过滤零匹配**；`--include='*.md'` 又让字面串扫描对 `.py` 半边结构性失明——而本轮的 C1 恰恰住在 `.py` 里。改为 `sed` 切节 + `--include` 双扩展名。 |
| **I5** 「当前条目」实为陈旧条目 | 接受。整节按可复现协议的全部步骤在新 HEAD 重跑重写，并立**重跑惯例**：节标题带轮次、条目绑当轮前驱 HEAD，让陈旧一眼可见。 |
| **M1–M7** | 逐条落地：SSOT 豁免三族**逐条列全**（不写「等」）；exit 2 补齐目录缺失分支；单行 docstring 加「行首起」限定；`本轮` 改显式轮次；清单竖线转义；PR 正文中提到 历史@4146c7a2 的那句已标明属 R12。 |
| **扫描词表同域** | 本轮把**英文关键词**（`pure ASCII` / `exactly two` / `every other .md`）纳入扫描记录——它们躲过了此前所有中文关键词扫描。 |

**第十一次复发的教训（入本记录，也入提交说明）**：
**每个修复必须对照其触碰文件的**全部**既有不变式**——本轮的复发不是新缺陷类，
而是一个修复（历史绑定）在满足自身目标的同时，撞穿了同一文件上的相邻不变式（纯 ASCII）。
检测器每强一轮，缺陷就迁一轴；这一次迁到了「修复动作本身」这一轴上。

## 整改记录（R19 终验，894fe0f8）

> **本节为该轮历史（历史@894fe0f8）**：节内现在时均指**当轮时点**，不描述现状；
> 机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

**第十二次复发**，位置极具教育意义：手抄不变量在**警告「脚本改了、抄本没改」的那段文字正下方**失同步。

| Finding | 处置 |
| --- | --- |
| **C1（Critical）脚本 SHA 三处抄本只同步了一处** | 接受。根因是同一个值有三份手抄本，而检查器豁免②「整行放行任意 64-hex」使它们**结构性无守护**——检查器**按设计**看不见陈旧的哈希。类修复：**不变量单点化**。脚本 SHA 与结果摘要在全库只允许存在于 `run-manifest.md`「本节保留的唯一两个不变量」小节；其余位置改指针；机器生成物（`boundary-cases.md`）与逐字粘贴的运行输出（`summary.md`）是**产物**不是手抄，例外并逐一具名。配**三条会变红的守护行**：SHA 与当场 `shasum` 比对、SHA 抄本数恰 1、摘要出现文件逐一具名。**手抄值唯一可行的守护，是让「有几处抄本」本身成为可执行的期望值。** |
| **I1** 检查器注释仍指向已撤回的白名单表 | 接受。R19 把 docstring 改成了指针，却漏了 60 行外的这条注释——**指针化必须按内容寻址扫全文件，不是只改开头**。新注释同时写明该豁免的代价：它放行任意 64-hex，因此陈旧抄本靠清单行守护，不靠这个文件。 |
| **I2** 扫描记录三格未复现 | 接受，且与 `summary.md:764` 记录的 R17 教训**完全同形**：填了表却没复跑。三格改为当场执行的真值（各 1，均为轮次域内自引用），shell 块期望注释同步，「三种记法」归属由 R18 节改正为 **R17 节**。填表纪律收紧：**凡命中不为 0 必须写明文件、行号与所属节**——只写「良性」而不给位置，等于要求复核者重做扫描才能证伪。 |
| **M1/M2/M3** | 竖线转义（全表格行改用 cell-count 核对，不再靠肉眼）；两处「显式白名单」改「显式的豁免集合（见 SSOT）」；`sed` 节排除改保留终止行的 `{…!d;}` 形式——旧写法把「### 落地核对清单」标题一并删掉，**排除范围多吃一行，下一节就对扫描半盲**（且 `;}` 不可省，BSD sed 会静默报错，已写进注释）。 |

## 整改记录（R20 终验，242f5991）

> **本节为该轮历史（历史@242f5991）**：节内现在时均指**当轮时点**，不描述现状；
> 机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

不变量单点化与三条守护行经变异测试确认真实可红。**第十三次复发**的机理是新的：
**不动点不稳定**——填表先于语料定稿，而修复某个字符串的整改叙事**必然要引用该字符串**。

| Finding | 处置 |
| --- | --- |
| **F1（Important）扫描格「三种记法」所填值低于当场执行输出** | 接受。多出的两处正是本提交自己的整改叙事。这不是填错，是**扫描域的语义错了**：轮次历史节里引用 finding 字符串是**构造良性**的——节头已声明历史作用域，节内引用按定义就是历史引用。类修复：**扫描域语义化**。域改为「全语料 − 扫描记录节 − 全部带历史作用域声明的节」，用一个五行 awk 状态机把历史节抹成空行（保留行号），不新建脚本、不改 `check_current_numbers.py`（冻结不破）。此后八格期望统一为 **0**，整改叙事再也不扰动计数——**不动点消失，而不是每轮重新去踩。** |
| **F2（Minor）「全表格行改用 cell-count 核对」是高估** | 接受。实际只转义了一处，独立解析仍有三行未转义（`run-manifest.md:261`、`summary.md:508`、`summary.md:597`），且**没有任何已提交的命令背书该声称**。三行已转义；核对命令落地为清单行。**过程中还撞见一个次生教训**：命令先被写进表格单元格，转义往返把 awk 正则改坏，逐字取出运行误报十余行——**能被格式转义改写的命令，不算已交付的命令**，故改放栅栏块，并按「整段复制即可执行」验证。 |
| **F3（Minor）外审轮次标签错** | 接受。`docs/reviews/` 下 PR #7 的外审文件对应 R1–R11，故下一轮是 **12**；「13」是内部轮次误加到外审计数上的。口径写进 `tasks.md` 并附核对命令 `ls docs/reviews/ \| grep -c pr-7`。 |

## 整改记录（R21 终验，4b48fb27）

> **本节为该轮历史（历史@4b48fb27）**：节内现在时均指**当轮时点**，不描述现状；
> 机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

机械层全绿（域语义化确认消除不动点、守护行变异可红、`cells.awk` 工作），
七项 finding 全部落在**报告层**。**第十四次复发**是同一条教训的第二次应验。

| Finding | 处置 |
| --- | --- |
| **F1（High）「当前条目」两轮未重跑** | 接受，这是本轮的类缺陷。标题停在 R19、前驱停在 历史@6507a986，而该节 **自己写着**「每轮必须整体重跑」——**它是散文义务，没有清单格子**。R19 刚记下「纪律必须变成清单格子才会被执行」，这条教训随即在**写下它的那一节**上第二次应验。类修复：新增**前驱守护行**——本节记录的前驱必须等于 `git rev-parse --short=8 HEAD^`，一红即证明本节没跟这轮重跑；轮次标签与前驱同处一节，故一个等式同时守住标签的新鲜度。本节已在本轮完整重跑重写。 |
| **F2（Med）PR 正文轮次表混两套编号** | 接受。R1–R11 是外审轮，`fd8032ca` 起的 R13–R21 数的是内部终验轮，中间**切换了轨道却无标注**——这正是 13-vs-12 误标的源头。表加**轨道列**，补上缺失的 **R12（内部终验）** 行与切换说明。 |
| **F3（Med）正文指针钉死旧 SHA** | 接受。指针钉在 `351c70aa`，其后十轮的整改记录从正文完全不可达；而「每轮更新这个 SHA」又是一条无守护的散文义务。改为指向 **PR HEAD 下的全部「整改记录」小节**，计数改为给出命令、正文不镜像数字。 |
| **F4（Med 潜伏）过滤器按任意行匹配** | 接受，且这是最危险的一条：按文档所述把它用到 `protocol-runs.md` 上，**讨论**该纪律的散文会把当前条目与落地清单整段抹掉，而八个格子**照样打印零**——**一个会致盲自己的过滤器比没有过滤器更糟**。锚定改为「`^## ` 节头后紧邻的首个非空行」；反例场景已作为清单行入库，当场实测新旧两版行为差异。 |
| **F5（Low）文档域与实现域不符** | 接受。措辞如实化：历史节排除**现只应用于** `summary.md` 与 `tasks.md`（现存历史节仅在此二文件）；`protocol-runs.md` 走 `sed`，删的是**扫描记录与关键词归类两个子节**，不是「本小节」。 |
| **F6（Low）轮次口径命令不可移植** | 接受，限定已补。（**当轮补的限定句本身是假的**：写成「`docs/reviews/` 有意不纳入版本控制」，而 `git ls-files docs/reviews` 列出十四个已跟踪文件，只有 `pr-7` 那批未跟踪。R24 改真。） |
| **F7（Low）`cells.awk` 基准取自块首行** | 接受。基准改从**分隔行**推导。当轮记下的机理句「坏表头整表静默放行」**未跑新旧对照**，R23 与 R24 两轮先后修正：坏表头的真实差异是**误归因 → 正确定位**；真正的静默洞是**各行一致的无分隔行表**（当轮交付的样本行内格数不一致，旧版对它报一条异常，并非零报告）。新旧对跑的逐字输出见 `evidence/protocol-runs.md` 扫描小节 shell 块的检测器对照段。 |

## 整改记录（R22 终验，c7aa54c1）

> **本节为该轮历史（历史@c7aa54c1）**：节内现在时均指**当轮时点**，不描述现状；
> 机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

机械层完全通过。五项报告层小 finding，其中 F-A 是**活体假声称**——最值得记的一条。

| Finding | 处置 |
| --- | --- |
| **F-A（Minor，活体假声称）「R21 坏表头整表静默放行」不成立** | 接受。当轮的改写只走到一半：「坏表头 = 误归因」已属实，但「缺分隔行由零报告变为报错」配的是一个**行内格数不一致**的样本，旧版对它报一条异常。R24 换成各行一致的无分隔行表并把**两版逐字输出**入库，见 `evidence/protocol-runs.md` 扫描小节 shell 块 4b 段。**同一条教训连吃两轮：写机理要跑对照，换样本也要跑对照。** |
| **F-B（Low）R1 两节缺作用域声明** | 接受。`summary.md` 与 `tasks.md` 的 R1 节没有历史声明，仍在扫描域内，使「历史节内引用构造良性」这句全称对它们**不真**（失败方向是假红，安全，但全称句有一个反例就不成立）。两节补上声明；并新增清单行**枚举所有未声明的 `##` 节**，输出必须只剩结构节——把「无遗漏」变成可执行断言，而不是又一次人工普查。 |
| **F-C（Nit）** | `grep -c` 的期望措辞由「输出为空」改为「输出为零」，与清单行同口径。 |
| **F-D（Nit）** | 正文指针句改为「同时包含外审轮与内部终验轮，轮次归属见轨道表」，不再自行复述编号——**复述就是再造一个会漂的镜像**。 |
| **F-E（Nit）** | 前驱守护命令从表格单元格移入栅栏块——它与 R21 自己写下的教训直接矛盾，却在同一份文件里并存了一轮。 |
| **终验信息项：根提交假绿** | 一并处理。守护改为**两侧非空**才比较：根提交无父时 `空 = 空` 会假绿。虽然本记录不可能位于根提交，但**守护行的正确性不该依赖「不可达」这种论证**。三种空值场景实测均 fail-closed。 |
| **顺带发现（自查）** | 守护原用 `grep '前驱 HEAD'` 提取，而同节惯例说明里也含该词，只是**恰好**不含十六进制串——靠「恰好」成立的提取等于没有锚。已改锚到带标签的整句。 |

## 整改记录（R23 终验，b347193f）

> **本节为该轮历史（历史@b347193f）**：节内现在时均指**当轮时点**，不描述现状；
> 机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

| Finding | 处置 |
| --- | --- |
| **#1（Important）静默洞样本不触发它要演示的场景** | 接受。当轮交付的 `nosep` 样本行内格数不一致，旧版对它报一条异常——**零报告只对各行一致的无分隔行表成立**。样本已换，新旧两版的逐字输出连同旧版 awk 一并入栅栏块；三处措辞改为引用该输出。 |
| **#2（Important）命令又写进表格单元格** | 接受。`\*\*` 的转义往返毁掉正则，逐字复制得七十余行 `live` 假红；且该行自称「同一 shell 块一并复跑」而命令根本不在块里。命令已移入块，表格行只留指针与期望。**这是同一教训第三次复发**，故立硬约束：**命令一律只住栅栏块**。 |
| **#3（Important）「`docs/reviews/` 有意不纳入版本控制」是假声称** | 接受。`git ls-files docs/reviews` 列出十四个已跟踪文件（PR #5、PR #6 轮的评审文件都入过库），只有 `pr-7` 那批恰好未跟踪。口径句改真，并把两条命令写进清单行。未移动或清理任何 reviews 文件。 |
| **#4（Minor）「认证横幅在首行」与代码保证相反** | 接受。认证输出首行是 `DIRECTED CASES` 头；横幅只出现于**未认证**输出的首行，且自检明确断言认证输出**不**以横幅开头。正文改真。 |
| **#5（Minor）两个「外审入口」节名冲突且过度声称** | 接受。合并为**一个**入口节并置于正文开头；声称改为「主栅栏块 + 块外三处 + 落地核对清单」；原节的声称与指针移入正文末的「声称范围与机制指针」小节。 |
| **#6（Minor）贴 `diff -rq` 输出却标 `diff -r`** | 接受。标签改为 `-rq`，并核对记录中的输出与当场执行**逐字一致**。 |

## 整改记录（R24 终验，cd9a1765）

> **本节为该轮历史（历史@cd9a1765）**：节内现在时均指**当轮时点**，不描述现状；
> 机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

| Finding | 处置 |
| --- | --- |
| **A（Blocking，既存）「八格 / 八个格子」是写死的漂移计数** | 接受。该措辞在 R22 写下时表已有十行，此后又长了一行，四处活体一直没跟着改，其中一处就在**主栅栏块的注释里**——审阅者复制即见。类修复：**去数字化**。四处一律改「全部字面串格 / 每一个字面串格」；需要数字的地方改为**计数命令 + 当场输出**：块内新增两条命令，分别数扫描命令条数与扫描表行数，**逐字期望是「两者输出同一个数」**，不写死那个数。正文同步去数并注明「不写死任何会漂的计数」。 |
| **B（Blocking）「未认证横幅在首行」跨模式不成立** | 接受。实测：`default` 模式下横幅在 `== CERTIFICATION ==` 节内、首行始终是 `== DIRECTED CASES -- CURRENT RULE ==` 头；「首行」这一性质只属 `--emit-markdown` 模式，也正是自检断言的那两条。正文缩域改真，按模式分述。 |
| **C（Nit）「块外还有三处」** | 实为两个脚本、五条命令。改真。 |
| **D（Nit）「约二十行」** | 去数字化，改为给出计数命令。 |
| **E（Nit）去镜像行的内容锚漏一个措辞** | 接受。锚只写了「差异统计」「与 Git 不符」，而两处命中用的是「第三次复发」。锚补齐为三选一，并把它变成**能变红的命令**：锚外命中数须为 `0`（命令入块，清单行留指针）。 |

## 整改记录（R25 终验，8429798c）

> **本节为该轮历史（历史@8429798c）**：节内现在时均指**当轮时点**，不描述现状；
> 机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

| Finding | 处置 |
| --- | --- |
| **F1（Blocking）「正文不写死任何会漂的计数」当页即被证伪** | 接受。同一页写着「那之后十轮」（实为十四）与「三条纪律」（`grep -c '^### .*纪律'` 得二）。**去数字化只做了词，没做全称**：改了被点名的那几处，没有对活体区做一次全量自审。两处改真——前者去数为「其后各轮」，后者去数并旁附计数命令。 |
| **F2（Blocking）「八格」处置文案两个半句都不成立** | 接受。「正文半句」无命令可红（PR 正文不在 `scan_corpus` 域内），「记录半句」被清单格「输出只剩八个结构节」当场证伪。格文案缩为**可检的记录半句**，正文半句改挂**入口彩排的人工对照义务**；那条清单格改为「输出只含结构节（当场枚举见块内输出）」。 |
| **R1** 单点化节说「两条守护行」实为三条 | 去数，改指清单（正文同处一并去数）。 |
| **R2** 「漏掉第三个措辞下的两处命中」实为一处 | 改真：补锚救回的是 `protocol-runs.md:14` 一处，另一处本就含「差异统计」。 |
| **R3** 「一一对应」是高估 | 降为**必要非充分**：删一条命令再加一条无关命令，计数仍相等；它挡的是「加了行忘了加命令」，不证明逐行对应。 |
| **R4** 清单行数命令连表头多算 | 改为 `n-2`（扣表头与分隔行），与当场数据行数一致。 |
| **R5** 「八轮的实测结论 / 八轮事实」已过时 | 去数为「历轮」。 |
| **类修复：数量断言自审（覆盖域如命令所示，非全称）** | 新增块内 4be 段：剥掉代码栅栏后，对**逐轮汇报面的散文**（protocol-runs 去两子节、summary/tasks 去历史节）跑「中文数字或阿拉伯数字 + 量词」邻接式扫描，豁免分五类且逐类写明理由，**期望无输出**。**已知盲区随命令一并披露**：英文数词、跨行拆分、未列量词、行级豁免的过宽性，以及被排除在域外的 `run-manifest.md` / `spec.md` / `customization-record.md`。踩到的坑一并入注释：`LC_ALL` 不设 UTF-8 时方括号表达式按字节匹配，会把每个汉字拆成三字节而全量误报（首次运行即如此）。PR 正文无法入域，故其义务写进入口彩排。 |

## 整改记录（R26 终验，8bea044a）

> **本节为该轮历史（历史@8bea044a）**：节内现在时均指**当轮时点**，不描述现状；
> 机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

**内部循环的最后一轮**：此后仍剩的 Minor/Nit 进 PR 正文的「已知残余」小节逐项披露，不再追。

| Finding | 处置 |
| --- | --- |
| **B1** `protocol-runs.md` 活体条目「两条守护行」实为三条 | 接受。这是 R25/R1 同一 finding 的**第二处原文**：当轮改了 `run-manifest.md` 就收工，没做全目录内容寻址 `grep`——**记录自己的修复纪律又一次没被执行**。`4be` 漏它是因为数字类没有「两」。现去数改指清单，并对该字符串做了全目录 `grep` 确认无第三处。 |
| **B2** 「六种形状全部被拒」实为七种 | 接受，同形：`removed begin` 在 历史@a48e9b65 就已加入，`run-manifest.md` 与 `summary.md` 都改过，**本节的副本没跟着改**。改为**逐一具名**、不写个数。`4be` 漏它是因为量词类没有「种」。 |
| **B3** 称 `4be` 为「全称自审」不成立 | 接受。三条轴未覆盖：「两」、量词 `[种个次步类族]`、数字与量词间的空白与 `**` 包裹。措辞统一降级为「数量断言自审（覆盖域如命令所示）」，并把已知盲区写在命令旁。 |
| **扩域后的域缩面（本轮的实质决定）** | 扩域后新命中集中在 `run-manifest.md`：两个流、五个分支标签、两份码点集、四个模式……**几乎全是构造常量**，由脚本内的交叉断言守护，改一处即 `AssertionError`。把它们逐个塞进白名单只会催生一张越来越长的无理由清单——那正是被反复证伪的方向。因此**把域缩到逐轮汇报面**，并把缩面本身写进「已知残余」披露，而不是假装闭合。 |
| **Minor 六项** | 全收：whitelist 补齐无理由条目、删死条目、修正「三个平台投影」的理由；域描述如实（点明「历史条目」节仍在域内）；行级豁免写进盲区披露；「R22、R23 两轮未触碰」改为**命令背书**（`git log -1 … carrier_sweep.py` 输出即真值）；PR 正文钉针 `summary.md@351c70aa` / `customization-record.md@351c70aa` 改跟随 HEAD（那份快照是**去镜像之前**的内容，点开会读到已被撤下的旧统计），而 `rules/project.md@4c0a6efc` 的钉针**保留**——那是条款生效的那一版，钉针在此是正确的；正文里那句以「当下」口吻描述的 `diff` 结论改为绑定其所在提交的时点表述；入口的块输出顺序改为与实际一致。 |

## 整改记录（外部第 12 轮复审，f5efce2）

> **本节为该轮历史（历史@f5efce28）**：节内现在时均指**当轮时点**，不描述现状；
> 机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

审阅记录：`docs/reviews/2026-07-29-pr-7-scaffold-self-adoption-rereview-f5efce2.md`，
结论 Request changes（16/24）。上轮三个 Important 与哨兵原文 Minor 经审阅方逐字复跑确认关闭。

| Finding | 处置 |
| --- | --- |
| **[Important] Python prose 扫描器未实现 SSOT 已声称的覆盖域** | 接受。审阅第 7 节的四个探针在旧实现上给出：`u docstring` `[]`、`R docstring` `[]`、`inline comment` `[]`、`assigned multiline string` `[(1,…),(2,…),(3,…)]`——**双向不符**：合法前缀的真实 docstring 与行内注释漏报，而赋值给名字的多行字符串反被当作 docstring 扫入，与轴表「代码字符串=开放」正相反。整改按裁决五条逐条落：**①域决策**取最宽自然语义——「`.py` prose = 全部注释（整行 + 行内）+ 真实 docstring（module/class/function/async，任意合法前缀与引号形态）」，赋值/调用/非首条语句的字符串明确开放，SSOT 措辞同步为此定义；**②实现重写**弃手写三引号状态机，改标准库 `tokenize`（COMMENT token，天然含行内注释）+ `ast`（`get_docstring` 对应节点的真实源码行范围，据此区分 docstring 与赋值字符串），不可 tokenize/parse 的 `.py` **fail closed**，计为具名违规而非静默跳过——这同时回应 Standards 的 Primitive Obsession 意见；**③④自检**新增独立语法形态矩阵 `PROSE_FORM_MATRIX`（bare/r/u/U/R × 单行/多行真 docstring、整行/行内注释、四类 docstring 宿主，加四条反向用例与一条 fail-closed 用例），**期望值逐行写死、不由被测函数生成**；审阅第 7 节的四个探针原样纳入并翻转；**⑤**重跑审阅记录第 4–9 节全部命令并绑定新 HEAD/CI。 |
| **顺带修正的一处反向假阳** | 旧实现把**追加在文件末尾**的三引号行当作 docstring 捕获，而按 Python 语法那是尾随表达式、不是 docstring。叉积注入据此改为：只有注释形态原样注入 `.py`，其余一律注释包装；真实 docstring 的覆盖改由形态矩阵在**语法给定的位置**证明。 |
| **[Minor] 检查器 docstring 复述 SSOT** | 接受，outline 段删除，只留权威指针与 usage。这是第二事实源纪律的又一实例：同一份 docstring 上半段说「不复述」，下半段就复述了。 |
| **[Minor] 提审提交数与 Git 真值不一致** | 接受。本轮及以后的整改报告一律附 `git rev-list --count` 的**命令输出**，不手写计数。 |

## 整改记录（R28 终验，fab34167）

> **本节为该轮历史（历史@fab34167）**：节内现在时均指**当轮时点**，不描述现状；
> 机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

checker 重写经终验全部核实。剩下的全是**外审记录落盘后轮次口径未刷新**与**节归属**问题——
共同的根因是：**前瞻句与外部事件驱动的计数，都不受历史作用域保护**。

| Finding | 处置 |
| --- | --- |
| **B1** PR 正文尾注仍写「下一轮外审是第 12 轮」 | 接受。表内已有外审 R12（`f5efce28`）行，`ls docs/reviews/ \| grep -c pr-7` 已推进。尾注改真，并写明该计数由「审阅方落盘外审记录」这一**外部事件**推进，每轮开头须复跑对应清单行。 |
| **B2** 外审轮次口径清单行在交付时即为红 | 接受，这条最值得记：外审记录落盘会改变第一条命令的计数，而该行的期望是上一轮的。**清单行的期望若依赖外部事件，就必须在下一轮开头复跑**——本行现记为 12 / 14 / 0，并把这条纪律写进格内。 |
| **B3** 新增的观察行插进了「历史条目：会话 C」 | 接受，**节级作用域纪律被本提交自己违反**：R28 的观察值被写进了一个绑定着更早 HEAD 的历史节里。已移入「当前条目」的机械观察表，`tasks.md` 那句「已入该条目」随之成真。 |
| **M1** 复选框与其下方说明自相矛盾 | 接受。复选框写「第 13 轮」，说明写「下一轮是 12；13 是误加」——双重过期。说明改真，并写明**前瞻句不受历史作用域保护**：历史域保护「当轮如此」，不保护「下一轮将如此」。 |
| **M2** SSOT 自检行漏了一整层 | 接受。同节下文说失败能力由「两层证明」，自检行却只列叉积变体。现补全为两层：形态矩阵（正向枚举 + 反向用例 + fail-closed 用例）与叉积注入（变体清单含新增的行内注释形态）。 |
| **M3** 函数 docstring 又复述域定义 | 接受，精简为一句 + SSOT 指针（`_docstring_lines` 同样精简）。第二事实源纪律在**同一个文件**里第三次出现。 |
| **M4** 前缀句未覆盖 `b` / `f` | 接受，补半句：二者的首条语句按 Python 定义就不是 docstring（`ast.get_docstring` 均返回 `None`，实测），故落在域外，实现与该定义一致。 |

## 整改记录（外部第 13 轮复审，b3fa77d）

> **本节为该轮历史（历史@b3fa77df）**：节内现在时均指**当轮时点**，不描述现状；
> 机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

审阅记录：`docs/reviews/2026-08-02-pr-7-scaffold-self-adoption-rereview-b3fa77d.md`，
结论 Request changes（15/24）。第 12 轮的四个探针经审阅方确认按字面关闭。

| Finding | 处置 |
| --- | --- |
| **[Important 1] 行号集合不是 prose 载体范围** | 接受，这条最值得记。上一轮把语法分类提升到了**行号集合**，却仍按行号回传整条物理行、对整行判定——于是同行的开放代码与受检 prose 互相污染，三个双向反例全部误报（数字或 marker 在开放代码侧，prose 侧并无断言）。**「域」不是一组行号，是一组源码片段。** 整改：COMMENT 取 token 的 `string`；docstring 按节点的 `lineno/col_offset/end_lineno/end_col_offset` 逐物理行切片，因此同行的 `def f(): "..."` 头部、`;` 之后的语句、注释左侧的代码都不进入片段；判定只作用于片段，行号仅用于报告定位。**旧反向矩阵只测孤立代码字符串，所以全绿**——反向用例必须把「同行还有 prose」这一维也覆盖到。 |
| **[Important 2] 解析前源码被改写** | 接受。此前先 `splitlines()` 再以换行重建才喂 tokenize/ast，等于**在解析前改写被解析的对象**：`str.splitlines()` 把 U+2028 当行边界，而 Python tokenizer 在注释内不当。两个方向都被击穿——合法注释被洗成代码（覆盖域漏报），不可解析源码被洗成可解析（绕过 fail closed）。整改：以 `tokenize.open` 读取**文件原始源码**直接喂解析器（原生识别编码声明与 BOM），行文本在解析之后按 Python 坐标模型派生。编码决策显式写入 SSOT。 |
| **[Minor] 函数 docstring 仍复述机制** | 接受。上一轮只删了 header outline，函数级没同步。三个函数的 docstring 精简为目的 + SSOT 指针；片段切片处的机制注释同样收为一句指针。第二事实源纪律在同一文件里的第四次。 |
| **自检口径升级** | 期望值由「行号集合」升级为**「载体片段 + violations」两列并逐行写死**；审阅列出的同行双轴用例、两个 U+2028 反例、以及 CRLF / UTF-8 BOM / 编码声明 / 无尾换行等读取层回归全部入矩阵。**只断言行号，正是当轮被击穿的地方。** |

## 整改记录（R30 终验，90845471）

> **本节为该轮历史（历史@90845471）**：节内现在时均指**当轮时点**，不描述现状；
> 机制现状见 `evidence/run-manifest.md` 的「检查器现状（SSOT）」节。

| Finding | 处置 |
| --- | --- |
| **B1（Blocking）片段边界用错了坐标单位** | 接受。Important 1 没被真正关闭，只是**从行空间搬到了列空间**：`ast` 的 `col_offset` 是 **UTF-8 字节偏移**，我拿它去切字符 `str`。ASCII 语料上恰好等价，中文语料上必然错位，三个方向都被击穿——(a) 片段吃进尾部代码，把开放代码里的数字连坐误报（外审 §8 的缺陷换个空间重现）；(b) 偏移越过行长，片段成**空串**，真 docstring 的断言**静默漏报且不 fail closed**；(c) 多行 docstring 末行泄漏尾部代码。整改：片段一律取自 `ast.get_source_segment`，由标准库负责偏移语义转换，**实现里不再出现任何列运算**。 |
| **矩阵为何没抓到** | 因为**所有双向用例的片段边界都落在 ASCII 上**——字符下标与字节偏移在那里恰好相等。另有一行的样本源码里根本没有字符串常量，什么都没断言。补三条非 ASCII 边界用例并修正该样本；可失败性由**把切片退回字符下标**的变异实测：恰好这三行变红、其余不变。**边界用例的边界，必须落在会出错的那类字符上。** |
| **B2（Blocking）残留旧复选框** | 接受。已勾的 R13 行、新增的第 14 轮行、未勾的旧第 13 轮行三者并存。删除旧行。前瞻标签类的第二次。 |
| **残余 1** `read_source` 在 fail-closed 之外 | 接受。编码声明不可满足的 `.py` 会抛裸 `SyntaxError` traceback 而非具名违规。读取移入 fail-closed 包裹，记为 `UNREADABLE`；两个复现（BOM + latin-1 cookie、未声明的非法字节）入矩阵。 |
| **残余 2** 词汇未同步 | 接受。`line_violations` → `fragment_violations`（参数与 docstring 同改）、`main()` 的输出改「.py prose fragments」、`read_source` docstring 收为一句 + SSOT 指针。 |
| **顺带发现：SSOT 漏更新** | R30 的 SSOT 编辑脚本中途断言失败即整体未写入，重跑时漏掉了「检查域」那一行，于是**实现已改而 SSOT 仍描述上一形态**。本轮补上，并把这一条作为 ∀ 审计新行披露：SSOT 与实现的同步**没有机械守护**。 |

## Decision

待独立审阅方在精确 HEAD 上裁定（Approve，可以合入 / Request changes，禁止合入），审阅记录归档于 `docs/reviews/`。
